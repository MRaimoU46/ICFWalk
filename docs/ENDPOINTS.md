# HTTP endpoints (Phases 1 to 5, the Phase 6 publish route, and Phase 7 reporting)

All routes are served through `index.cfm`; with the ColdFusion connector's default configuration the
paths below are reached as `/index.cfm/api/...` (the HTML shell as `/index.cfm/`). Static assets
(`app/assets/css`, `app/assets/js`) are served by the web server directly at `/assets/...`. Responses are JSON (`application/json; charset=utf-8`)
serialized in canonical form. Every response carries `X-Correlation-Id`.

Errors: `{ "error": { "code": "...", "message": "...", "correlationId": "...", "details": {...} } }`.

## Authorization policies

Every route declares one policy in `src/http/Router.cfc`:

| Policy | Meaning |
| --- | --- |
| `public` | No identity required (health only). |
| `maintenance` | Operator token guard: `X-ICFWalk-Maintenance-Token`, explicit enable flag, loopback caller unless remote access is allowed. No session, no CSRF. Denials are 404. |
| `{ authenticated }` | A signed-in user (identity asserted by the configured adapter), with or without roles. |
| `{ permission }` | A signed-in user holding the permission (`instrument.manage` globally, or an org-scoped permission for the unit named by the route). Record-level checks (walk owner/scope) happen in controllers through `AuthorizationService.authorizeWalk`. |
| `{ anyPermission }` | A signed-in user holding at least one of the listed permissions anywhere. Used for shared, non-record resources (the HTML shell and the instrument render model). Denials are audited `ACCESS_DENIED`. |

Status codes on denial: 401 `UNAUTHENTICATED` (no identity), 403 `FORBIDDEN` (capability missing),
403 `CSRF_TOKEN_INVALID`, 404 `NOT_FOUND` (record or unit outside the caller's scope, so existence
is not disclosed).

Session-authenticated state-changing requests (POST/PUT/PATCH/DELETE) must send the session's token
in `X-ICFWalk-CSRF-Token` (from `GET /api/me` or `GET /api/auth/csrf-token`).

Identity headers by adapter (`ICFWALK_SSO_MODE`):

- `header` (production): the SSO gateway sends `X-Auth-Subject` (+ optional `X-Auth-Name`,
  `X-Auth-Email`, `X-Auth-Proxy-Secret`); header names are configurable. Only requests from
  `ICFWALK_SSO_TRUSTED_PROXIES` are believed.
- `development` (dev/test only): `X-ICFWalk-Dev-Subject` (+ `X-ICFWalk-Dev-Name`, `X-ICFWalk-Dev-Email`).

## Routes

| Method | Path | Policy | Purpose |
| --- | --- | --- | --- |
| GET | `/` (i.e. `/index.cfm/`) | anyPermission walk.create, walk.read, walk.edit_owned, report.view, instrument.manage | The single HTML page for My Walks, the walk editor and (Phase 7) Reports (`src/views/shell.html`); a report-only role lands on Reports and the page never calls a walk route for it. Contains no instrument content or user data; the browser loads `/api/me` and `/api/instrument/current`. Strict `Content-Security-Policy` (scripts and styles from this origin; Google Fonts and the district logo host allowed), `X-Frame-Options: DENY`, `no-store`. Browsers (`Accept: text/html`) receive HTML error pages for 401/403/404 on this route; APIs always answer JSON. |
| GET | `/api/instrument/current` | anyPermission (same list) | `{ version: { versionId, versionLabel, status, checksum, publishedAt, isFallbackDraft }, policies: { hiddenDimensionPolicy }, model, correlationId }`. `model` is the render model (`icfwalk-render-model/1`, see `docs/ARCHITECTURE.md`) of the newest PUBLISHED version, or of the newest DRAFT when `ICFWALK_ALLOW_UNPUBLISHED_INSTRUMENT` permits (never in production). 404 `INSTRUMENT_NOT_AVAILABLE` when no renderable version exists. Read-only; contains no walk data and no SQL identifiers. |
| GET | `/api/health` | public | Liveness/readiness: `{ application, status, checks: { database, schema }, correlationId }`. 503 when the database is unreachable. Non-production adds `environment` and `engine`. |
| GET | `/api/me` | authenticated | `{ user: { userId, displayName, email }, identityProvider, permissions, orgUnits, assignments, csrfToken, correlationId }`. `permissions` maps `walk.create`, `walk.read`, `walk.edit_owned`, `report.view` to arrays of covered org unit ids and `instrument.manage` to a boolean. |
| GET | `/api/auth/csrf-token` | authenticated | `{ csrfToken }`. |
| POST | `/api/auth/sign-out` | authenticated + CSRF | Invalidates the server session; audited. |
| GET | `/api/admin/instrument/versions` | permission `instrument.manage` | Instrument versions with status, checksum, timestamps, publisher, walk counts. |
| POST | `/api/admin/instrument/versions/{versionId}/publish` + CSRF | permission `instrument.manage` | **Phase 6 (ADM-03/04/05).** Freezes one DRAFT into a PUBLISHED version. **Takes no request body**, enforced on the raw request rather than on the parsed struct: the route asks whether any body bytes arrived (`Content-Length`, falling back to the parsed content for a chunked request), because no body and a literal `{}` both parse to an empty struct. No body is accepted; `{}`, `{ }` and whitespace-only bytes are refused 400 `PUBLISH_BODY_NOT_ALLOWED`; `null` and `[]` are refused 400 `INVALID_JSON_BODY`; any object, including one naming an actor, publisher, checksum or snapshot, is refused 400 `PUBLISH_BODY_NOT_ALLOWED`. `Content-Length: 0` is not a body. A body is refused rather than ignored, so a client cannot believe it supplied an actor, a publisher, a checksum or a snapshot. Under the version row's `UPDLOCK, ROWLOCK` -- the same lock, in the same order, every definition write takes -- the service proves: the version is a DRAFT; the publisher is a real `icf.app_user`; a compiled snapshot exists and parses; its SHA-256 is the stored `checksum_sha256`; the parsed document is a JSON **object** and not `null`, an array, a string, a number or a boolean (`SNAPSHOT_SHAPE` at `$`, established before anything dereferences or serializes it, so a top-level `null` -- which leaves the parsed variable null on Lucee -- is a documented 422 with a durable refusal audit rather than a 500 with none); the stored snapshot envelope is the one the runtime reads (declared format, `definitions`, `instrument`, `version`, a `counts` block agreeing with its own definitions) and names *this* version and instrument; the stored bytes are the canonical serialization of the document they parse to (`SNAPSHOT_NOT_CANONICAL`; publication refuses rather than rewriting, which would move the reviewed checksum); the `counts` block carries all nine required members, each an actual JSON **number** -- not a numeric-looking string, which CFML's `isNumeric` and loose comparison used to accept -- that is whole, non-negative, within the supported integer range, and equal to the definitions' own (`SNAPSHOT_COUNTS_INVALID` for a wrong type or an out-of-contract value, `SNAPSHOT_COUNTS_MISMATCH` for a disagreement); the definitions inside the snapshot are semantically valid; the definitions SQL Server holds are semantically valid *in their own right*; the real runtime renderer builds that exact snapshot completely, so a version that satisfies every rule but that `RenderModelBuilder` throws on -- or builds with content silently dropped -- is refused here rather than frozen and discovered later as a 500; and the two definition sets still compile to the same definitions checksum. Both definition sets are validated separately on purpose -- agreeing with each other is exactly what a corrupted DRAFT does. Publication then writes status, snapshot, checksum, publisher, `published_at` and `effective_start` in one statement, and asserts the stored publisher is the signed-in user before auditing. Nothing is recompiled: the stored bytes and checksum are what the import produced. 200 `{ versionId, versionLabel, instrumentId, instrumentCode, status, checksum, definitionsChecksum, publishedByUserId, counts, placeholderCount, elapsedMs }`. 400 `INVALID_VERSION_ID` (malformed id), 400 `PUBLISH_BODY_NOT_ALLOWED`, 400 `PUBLISHER_REQUIRED` / `PUBLISHER_INVALID` / `PUBLISHER_UNKNOWN` (service-level; unreachable through this route, which always supplies the authenticated principal), 401 `UNAUTHENTICATED`, 403 `FORBIDDEN`, 403 `CSRF_TOKEN_INVALID`, 404 `INSTRUMENT_VERSION_NOT_FOUND` (existence not disclosed), 409 `INSTRUMENT_VERSION_NOT_DRAFT` with `details.status` (already PUBLISHED or RETIRED -- also what a repeat publication and a lost-response retry receive), 422 `INSTRUMENT_VERSION_NOT_PUBLISHABLE` with `details.issues[] = { code, message, path }`. Every refusal leaves status, timestamps, publisher, snapshot, checksum, definitions and every row version exactly as they were, and writes exactly one `INSTRUMENT_VERSION_PUBLISH_REFUSED` audit event **after** the rollback (a record written inside the transaction would roll back with it). Success audits `INSTRUMENT_VERSION_PUBLISHED` once, with the stored publisher as actor. Two concurrent publishes of one version produce exactly one transition and one `INSTRUMENT_VERSION_NOT_DRAFT`; audit details carry identifiers, counts and checksums only, never definitions or narrative content. |
| GET | `/api/walks` | anyPermission walk.create, walk.read, walk.edit_owned | My Walks: `{ walks: [WalkHeader + state.dimensions], scope }`. Default `scope=mine` (own non-voided walks in units where the caller holds walk.read or walk.edit_owned); `?scope=all` adds every non-voided walk in walk.read units (never drafts of other districts). Newest `updated_at` first, at most 500. Responses are not included in the list. |
| POST | `/api/walks` + CSRF | permission `walk.create` for body `orgUnitId` | Creates a DRAFT pinned to the current instrument version and the signed-in owner. Body `{ orgUnitId, clientMutationId, versionId?, dimensions?, responses? }`. `clientMutationId` is required (400 `CLIENT_MUTATION_ID_REQUIRED` / `CLIENT_MUTATION_ID_INVALID`); `rowVersion` is not, because there is no prior row. `dimensions` and `responses` may both be omitted (the engine's blank state) but never just one (400 `STATE_CONTAINER_REQUIRED`). 201 `{ walk }`; 200 with `walk.replayed=true` when the mutation id was already committed (WALK-03) -- the replay re-authorizes the recorded walk first, so an id recorded before access changed answers 404 rather than disclosing it; 409 `MUTATION_ID_REUSED` when the id was committed for a different semantic request; 409 `INSTRUMENT_VERSION_CHANGED` when `versionId` is not the current version (a replay is not subject to this: a create committed against an earlier version still replays after a newer one is published); 409 `SCHOOL_ORG_MISMATCH` when the School dimension contradicts the walk's mapped SCHOOL org unit, 409 `SCHOOL_ORG_UNMAPPED` when that unit has no validated School mapping; 409 `MUTATION_REPLAY_SUPERSEDED` when the recorded walk has changed since the mutation committed; 409 `MUTATION_LEGACY_UNVERIFIABLE` for a pre-migration-004 mutation row; 404 for an out-of-scope or inactive unit. |
| GET | `/api/walks/{id}` | anyPermission walk.read, walk.edit_owned; then `authorizeWalk(read)` | The walk aggregate: header (`id, orgUnitId, orgUnitName, versionId, versionLabel, status, ownerUserId, ownerDisplayName, isOwner, canEdit, observedAt, createdAt, updatedAt, completedAt, voidedAt, rowVersion, revisionCount`), `state` (`dimensions` by code, `responses` by item key, docs/DATA_CONTRACT.md shape), `states` (`responseStates`, `dimensionStates` derived by the server engine, `persistedResponseStates` as stored). 404 outside scope (existence not disclosed), 403 without any walk capability, 400 for a malformed id. |
| GET | `/api/walks/{id}/instrument` | same as open | `{ version: { versionId, versionLabel }, policies, model }`: the render model of the walk's pinned version (WALK-11), authorized through the walk, not by version id. |
| GET | `/api/walks/{id}/summary` | anyPermission walk.read, walk.edit_owned; then `authorizeWalk(read)` | **Phase 5.** The walk's text summary as a download. Body: `text/plain; charset=utf-8`, UTF-8 without a BOM, LF line ends, **no trailing newline**, exactly the bytes `WalkSummaryFormatter.summaryText` produces for the walk as stored (the same contract `app/assets/js/summary.js` implements; `tests/fixtures/summary-vectors.json` proves the two byte-identical). Built from the walk's **pinned** instrument version (WALK-11) and the server engine's evaluation, so a dimension or section the instrument currently hides is excluded from the text even though the value is retained (`docs/DATA_CONTRACT.md`, "Visibility and clearing rules"). Headers: `Content-Disposition: attachment; filename="ICFWalk_<label>.txt"` where `<label>` is sanitized to `[A-Za-z0-9_-]` only (SUM-05: no quote, path separator, or traversal sequence can reach it), `Cache-Control: no-store`, `X-Content-Type-Options: nosniff`, `X-Correlation-Id`. Read-only: no CSRF, nothing is written, no row version moves, no mutation is recorded. **Materialized under the walk mutation lock**: the header, dimension values, responses, visibility evaluation, text and file name are all produced inside one transaction that takes `findWalk(id, true)` (`WITH (UPDLOCK, ROWLOCK)`) first, the same lock SAVE, COMPLETE and VOID each take before anything else, so no mutation can commit part-way through an export and the file always describes one committed state of the walk (`docs/DATA_CONTRACT.md`, "Summary export coherence"). The audit event and log line are written after that transaction, from the materialized result. A VOIDED walk still exports (Phase 4 keeps it readable by id) and the text carries no status line. 403 without a walk capability (report-only and instrument-admin roles included), 404 outside the caller's organizational scope (existence is not disclosed) and for an unknown id, 400 for a malformed id. Audits `WALK_SUMMARY_EXPORTED` with `{ status, versionId, bytes }` only -- never the text, a note, a subject, a body, or a recipient (SEC-05). Nothing about the request selects what is exported: no version id, org unit, item list, or file name is accepted. |
| PUT | `/api/walks/{id}` + CSRF | anyPermission walk.edit_owned; then `authorizeWalk(edit)` (owner in scope) | Autosave of the whole working state: `{ rowVersion, clientMutationId, dimensions, responses, walkId?, versionId?, changedAt? }`. `rowVersion`, `clientMutationId`, and both state containers are required (400 `ROW_VERSION_REQUIRED/INVALID`, `CLIENT_MUTATION_ID_REQUIRED/INVALID`, `STATE_CONTAINER_REQUIRED/INVALID`). Validated against the pinned version (400 `UNKNOWN_ITEM`, `UNKNOWN_DIMENSION`, `INVALID_OPTION`, `INVALID_DIMENSION_VALUE`, `INVALID_DATE`, `INVALID_RESPONSE_VALUE`, `INVALID_EMAIL_DRAFT`, `VALUE_TOO_LONG`, `VERSION_MISMATCH`, `WALK_ID_MISMATCH`, `CLIENT_STATE_NOT_ACCEPTED`; `details.issues[]` lists every problem). JSON primitive types are checked, never coerced: a code, text, or date must arrive as a JSON string, a number as a JSON number, a boolean as a JSON boolean. Response `state` is derived by the server and refused from a client. 409 `STALE_ROW_VERSION` with `details.serverRowVersion` (nothing written), 409 `WALK_VOIDED`, 409 `MUTATION_ID_REUSED`, 409 `MUTATION_REPLAY_SUPERSEDED`, 409 `MUTATION_LEGACY_UNVERIFIABLE`, 409 `SCHOOL_ORG_MISMATCH`, 409 `SCHOOL_ORG_UNMAPPED`, 400 `WALK_COMPLETION_INVALID` (a completed walk must stay complete). 200 `{ walk }` with the committed `rowVersion`, normalized `state`, `states`, `changes[]` (server clearing decisions), `savedAt`, `clientMutationId`, `replayed`. An identical save to a COMPLETED walk is a no-op: no revision, no new `rowVersion`. |
| POST | `/api/walks/{id}/complete` + CSRF | anyPermission walk.edit_owned; then `authorizeWalk(edit)` | `{ rowVersion, clientMutationId }`, both required. Server validates every required, visible item/dimension: 400 `WALK_INCOMPLETE` with `details.errors[] = { kind, key, sectionKey, questionNumber, message }` (the draft stays saved); 409 `STALE_ROW_VERSION`, `WALK_ALREADY_COMPLETED`, `WALK_VOIDED`, `MUTATION_REPLAY_SUPERSEDED`, `MUTATION_LEGACY_UNVERIFIABLE`. Success appends revision `COMPLETE`, sets `COMPLETED`/`completed_at`, audits `WALK_COMPLETED`. |
| POST | `/api/walks/{id}/void` + CSRF | anyPermission walk.edit_owned; then `authorizeWalk(void)` | `{ rowVersion, clientMutationId, reason? }`. `rowVersion` and `clientMutationId` are required (400 `ROW_VERSION_REQUIRED/INVALID`, `CLIENT_MUTATION_ID_REQUIRED/INVALID`); `reason` must be a JSON string (400 `INVALID_VOID_REASON`). Drafts void with the default reason (the My Walks delete action); a COMPLETED walk needs `reason` (400 `VOID_REASON_REQUIRED`). 409 `STALE_ROW_VERSION` (the walk is not voided and its row version does not move), `WALK_ALREADY_VOIDED`, `MUTATION_ID_REUSED`, `MUTATION_REPLAY_SUPERSEDED`, `MUTATION_LEGACY_UNVERIFIABLE`. Audits `WALK_VOIDED`; rows are retained. |
| DELETE | `/api/walks/{id}` + CSRF | anyPermission walk.edit_owned; then `authorizeWalk(void)` | Always 409 `WALK_DELETE_REFUSED` (WALK-08): walks are never physically deleted. Audited. |
| GET | `/api/reports/options` | permission `report.view` (any unit) | **Phase 7; RPT-03 correction.** What the caller may filter a report by: `{ format: "icfwalk-aggregate-report/2", version, versions[], orgUnits[], dimensions[], sections[], items[], statuses, disclosure: { minimumWalks, liveAvailable }, releases[], canRelease, correlationId }`. `versions` are the configured instrument's PUBLISHED and RETIRED versions plus the current one (outside production, possibly the DRAFT preview), each `{ versionId, versionLabel, status, isCurrent }`; `version` is the selected one (`?versionId=`, default the current version). `orgUnits` are exactly the active units the caller's report assignments cover (effective dates and `include_descendants` resolved), `{ orgUnitId, code, name, type, parentOrgUnitId }`. `dimensions` are the version's reportable, non-sensitive controlled-list dimensions except School, with their value codes and labels; `items` are its reportable single-choice items `{ itemKey, sectionKey, prompt, questionNumber, layout, isPlaceholder, scoreEnabled, options[] }`; `sections` are the sections that contain one. `disclosure.minimumWalks` is the approved minimum (3 unless configured higher); `liveAvailable` is whether the caller may have live figures over their whole scope (they hold `walk.read` on every covered unit). `releases` are every release, latest dates first, `{ releaseId, observedFrom, observedTo, minimumWalks, releasedAt, versionIds }`, where `versionIds` are the versions the release holds blocks of in the caller's units. `canRelease` is whether the caller may create a release. Instrument content, scope and release metadata only -- no walk data. The only accepted parameter is `versionId`: anything else is 400 `REPORT_FILTER_UNKNOWN`; 400 `INVALID_VERSION_ID`, 404 `REPORT_VERSION_NOT_FOUND` / `REPORT_VERSION_NOT_AVAILABLE`, 401, 403 (no `report.view`: instrument administrators and role-less users). |
| GET | `/api/reports/aggregate` | permission `report.view` (any unit); the population is drawn from the caller's covered units only | **Phase 7 (RPT-01..07); RPT-03 correction.** One aggregate report over the walks of **one** instrument version, either **live** or from a **release** (`docs/DATA_CONTRACT.md`, "Aggregate privacy rule (RPT-03)"). Query parameters, all optional, every one re-validated on the server: `versionId` (default current); `orgUnitId` (a unit the caller covers -- the report draws on it and its covered descendants; any other id is 404 and audited `ACCESS_DENIED`, so existence is not disclosed; checked before anything below); `releaseId` (read that release); `from` and `to` (`YYYY-MM-DD`, inclusive, on the walk's observation date); `dim_<code>=<valueCode>` for each reportable dimension (`dim_grade`, `dim_content`, `dim_period`, `dim_classType`, `dim_visitTiming` in the current instrument; the parameter name matches case-insensitively, the value code exactly); `section` and `item` (restrict which items are reported, not who is counted); `optionItem` + `option` together (only walks whose response to that item is ANSWERED with that option); `includeDrafts=true` (default: COMPLETED walks only; VOIDED walks are never counted). Empty values are absent filters. **Live** (no `releaseId`) is served only when the caller holds `walk.read` on every unit the report would count; otherwise 400 `REPORT_RELEASE_REQUIRED`, whatever the other parameters. **Released** (`releaseId`) takes `versionId`, `orgUnitId`, `section` and `item` only: `from`, `to`, any `dim_*`, `optionItem`/`option` and `includeDrafts` are 400 `REPORT_FILTER_NOT_PERMITTED` (one issue per parameter); it counts COMPLETED walks of blocks with at least the release's minimum walks, protected cell by cell. 200 `{ format: "icfwalk-aggregate-report/2", generatedAt, mode: "LIVE" \| "RELEASE", release: null \| { releaseId, observedFrom, observedTo, minimumWalks, releasedAt }, disclosure: { minimumWalks, protected }, version, filters (including releaseId), filterCount, scope: { orgUnitCount }, population: { walks, byStatus, withheld }, orgUnits[]: { orgUnitId, code, name, type, walks }, dimensions[]: { code, label, states: { ANSWERED, UNANSWERED, HIDDEN }, withheldStates[], withheldResponses, values[]: { code, label, walks, withheld } }, sections[]: { sectionKey, title, parentSectionKey, depth, itemKeys, scored: { responses, sum, mean, withheld } \| null }, items[]: { itemKey, sectionKey, prompt, questionNumber, layout, isPlaceholder, scoreEnabled, options[]: { code, label, numericScore, isNa, count, withheld }, states: { ANSWERED, UNANSWERED, HIDDEN, NOT_APPLICABLE, UNRECORDED }, withheldStates[], withheldResponses, scored: { responses, sum, mean, withheld } \| null }, attempts, correlationId }`. Option counts are ANSWERED responses only; `scored.mean` is `sum / responses` over answered, scored, non-N/A responses (four places), `null` when nothing was scored; a section's `scored` pools every reported scored response beneath it, so it is weighted by responses, never an average of walk averages. A dimension value hidden by the instrument's rules (Period outside grades 6-12, retained under `RETAIN_HIDDEN`) counts as HIDDEN, never as its value, and never matches a filter. In a released report a withheld figure is `null` (never 0, never a number beside a flag), a partly published figure is its published part with `withheld: true` (and its state named in `withheldStates`), and a mean on fewer than the minimum published ratings has `responses`, `sum` and `mean` all `null`; a selection with no released block is `population: { walks: null, byStatus: {}, withheld: true }` with nothing categorical, identically for 0, 1 or 2 walks. In a live report nothing is withheld (`withheld: false`, empty `withheldStates`, `withheldResponses: 0`). **Never** returned: a walk id, owner, observer, note, email draft, free text, "Other" text, teacher field or classroom label. Errors: 400 `REPORT_FILTER_UNKNOWN`, `REPORT_FILTER_VALUE_INVALID`, `REPORT_FILTER_INCOMPLETE`, `REPORT_DATE_RANGE_INVALID`, `REPORT_FILTER_NOT_PERMITTED`, `REPORT_RELEASE_REQUIRED`, `INVALID_ORG_UNIT`, `INVALID_VERSION_ID`, `INVALID_RELEASE_ID` (each with `details.issues[] = { parameter, code }`); 401; 403 without `report.view`; 404 for an out-of-scope or unknown unit, `REPORT_VERSION_NOT_FOUND` and `REPORT_RELEASE_NOT_FOUND`; 409 `REPORT_POPULATION_CHANGED` with `details.attempts` when walks in a live population kept changing during three consecutive attempts (see `docs/DATA_CONTRACT.md`, "Aggregate report coherence"). Read-only: no CSRF, nothing written; logged `report.generated` with `{ versionId, mode, walks, items, attempts, filters, ms }` (`walks` is `-1` when withheld). |
| GET | `/api/reports/aggregate.csv` | same as `/api/reports/aggregate` | **Phase 7; RPT-03 correction.** The same report as an RFC 4180 CSV download, built from the same report the JSON route returns, so it never carries a figure that route withholds: UTF-8 with a byte order mark, CRLF line ends, header `record_type,group,key,label,count,withheld,scored_responses,score_sum,mean`, and one record per figure: `META` facts (format, mode, version, statuses, `minimum_walks`, the release's id and dates when there is one, each filter), `POPULATION`, `ORG_UNIT`, `DIMENSION_STATE` (ANSWERED, UNANSWERED, HIDDEN, and WITHHELD_RESPONSES when non-zero), `DIMENSION_VALUE`, `SECTION` and `ITEM` (scored figures, with `withheld` flagging the mean), `ITEM_STATE` (ANSWERED, UNANSWERED, HIDDEN, NOT_APPLICABLE, UNRECORDED, WITHHELD_RESPONSES) and `OPTION`. An empty count with `withheld` = 1 is withheld; a count with `withheld` = 1 is only the published part. Every text cell a spreadsheet would evaluate (leading `=`, `+`, `-`, `@`, tab or CR) is prefixed with an apostrophe, and cells containing a comma, quote or line break are quoted with doubled quotes. Headers: `Content-Type: text/csv; charset=utf-8`, `Content-Disposition: attachment; filename="ICFWalk_report_<version label>_<yyyymmdd>.csv"` built from `[A-Za-z0-9_-]` only, `Cache-Control: no-store`, `X-Content-Type-Options: nosniff`. Audited `REPORT_EXPORTED` with `{ versionId, mode, walks, withheld, rows, bytes, filters, attempts, orgUnitId?, releaseId? }` only (`walks` is `-1` when withheld). Same errors as the JSON route, answered as JSON. |
| POST | `/api/reports/releases` | permission `report.view`, CSRF; the service further requires `walk.read` and `report.view` on **every** active org unit | **RPT-03 correction.** Freezes one range of dates for report-only users. Body exactly `{ observedFrom, observedTo }` (`YYYY-MM-DD`, inclusive); anything else in it is 400 `REPORT_RELEASE_BODY_INVALID`, and who releases is the signed-in principal, never the body. The dates must be valid (400 `REPORT_RELEASE_DATES_INVALID`), in order, and have passed -- the last before today, UTC (400 `REPORT_RELEASE_DATES_OPEN`) -- and must not overlap any existing release (409 `REPORT_RELEASE_OVERLAP`; the database trigger refuses it too; 409 `REPORT_RELEASE_BUSY` if another release held the lock for 30 seconds). A caller who cannot open every walk in every school is refused (403 `REPORT_RELEASE_NOT_PERMITTED`, audited `ACCESS_DENIED`). Under an exclusive lock, every reportable version's COMPLETED walks observed on those dates are read as one coherent population (409 `REPORT_POPULATION_CHANGED` after three moving attempts) and every (version, org unit) block with at least the configured minimum walks is stored with its breakdown counts; smaller blocks are not stored. 201 `{ release: { releaseId, observedFrom, observedTo, minimumWalks, releasedAt, versions[]: { versionId, blocks }, blocks }, correlationId }`. Audited `REPORT_RELEASED` and logged `report.released` with `{ releaseId, observedFrom, observedTo, minimumWalks, versions, blocks, attempts }`. A release is never updated or deleted. Other methods: 405. |
| POST | `/api/maintenance/instrument/import` | maintenance | Imports the instrument configuration as a DRAFT (`config/instrument-config.json` or `{ "configFile": "name.json" }`). Optional `instrumentCode` and `versionLabel` import the same document under a different identity; both are plain overrides (1-60 and 1-100 characters, 400 `INSTRUMENTCODE_INVALID` / `VERSIONLABEL_INVALID`) that reach no other document field, and the full validation runs on the result either way. They exist so the publish path can be verified end to end without publishing -- and thereby freezing -- the seeded DRAFT every other check reads. 201 created / 200 updated; 422 `INSTRUMENT_CONFIG_INVALID`; 409 `INSTRUMENT_VERSION_IMMUTABLE` / `INSTRUMENT_VERSION_IN_USE`. |
| GET | `/api/maintenance/instrument/versions` | maintenance | Same listing as the admin route, for operators without a session. |
| POST | `/api/maintenance/instrument/discard-draft` | maintenance | `{ "versionLabel", "instrumentCode"? }` deletes a DRAFT no walk references. The lookup is scoped by instrument identity as well as by label (labels are unique only within an instrument); `instrumentCode` defaults to `ICFWALK_INSTRUMENT_CODE`. 404 `VERSION_NOT_FOUND` when that instrument has no such label, `INSTRUMENT_NOT_FOUND` for an unknown code. |
| POST | `/api/maintenance/org-units/import` | maintenance | `{ "orgUnits": [ { code, type, name, parentCode, active?, schoolValueCode? } ] }` or `{ "file": "org-units.example.json" }`. Idempotent upsert by code, parents resolved in a second pass. `schoolValueCode` on a SCHOOL unit declares the instrument School dimension value that names it (`icf.org_unit_dimension_map`, migration `005`), validated against the School dimension of the current renderable version and stored in the instrument's own spelling: 400 `ORG_UNIT_SCHOOL_VALUE_UNKNOWN` for a value the instrument does not define, 400 `ORG_UNIT_DIMENSION_VALUE_TAKEN` for one another unit already holds (it is never moved), 400 `ORG_UNIT_SCHOOL_VALUE_NOT_SCHOOL` on a non-SCHOOL unit, 400 `ORG_UNIT_SCHOOL_VALUE_NOT_IDENTIFYING` for the dimension's free-text option (`other`), which the instrument defines but which names no school. Answers `{ imported, created, updated, schoolValuesMapped }`. |
| POST | `/api/maintenance/org-units/align-school-dimension` | maintenance | `{ "dryRun"?: true, "confirm"?: [ { orgUnitCode, valueCode } ] }`. **Reports** the School mapping an operator could derive for active SCHOOL units whose `org_unit_code` is exactly a School dimension value code of the current renderable version, as `candidates[]`; code equality on its own persists nothing. Only the pairs sent back in `confirm[]` are written (`source` `CODE_ALIGNED`), each re-derived and re-validated first, and always as the instrument spells the code. An `EXPLICIT` row is never overwritten and a value another unit holds is never moved. Answers `{ versionId, dimensionCode, dryRun, confirmationsReceived, candidates[], mapped[], alreadyMapped[], unmapped[], refused[] }`; `unmapped[]` names every SCHOOL unit left without a mapping and why (`NO_MATCHING_VALUE_CODE`, `NON_IDENTIFYING_VALUE_CODE`, `VALUE_MAPPED_TO_ANOTHER_UNIT`) and `refused[]` every confirmation not written and why (`NOT_A_CANDIDATE`, `CONFIRMATION_DOES_NOT_MATCH_CANDIDATE`). A unit coded `other` is the dimension's free-text option, never a candidate and never confirmable. Idempotent; a dry run ignores confirmations. This is the only place an org-unit code is compared with a dimension value code, and the walk path reads only the row it stores. 400 `ALIGN_CONFIRM_INVALID` for a malformed `confirm[]`; 404 `INSTRUMENT_NOT_AVAILABLE` without a renderable version. |
| POST | `/api/maintenance/identity/provision-user` | maintenance | `{ subject, displayName?, email? }` creates the application account (201) or returns the existing one (200). |
| POST | `/api/maintenance/identity/assign-role` | maintenance | `{ subject, roleCode, orgUnitCode, includeDescendants?, effectiveStart?, effectiveEnd? }` (ISO-8601 instants). 201 with the assignment id. |
| POST | `/api/maintenance/identity/cleanup-fixtures` | maintenance, tests enabled only | `{ tag }` removes test fixture users, org units, walks, and **instrument versions of instruments whose own code starts with `tag-`** (whatever status those versions reached; the seeded ICFWalk instrument is never in scope). This is the verification harness for a path that has no production deletion equivalent -- `DefinitionRepository` refuses to delete a PUBLISHED or RETIRED version, by design -- and it is reachable only where `ICFWALK_TESTS_ENABLED` is on, which `ConfigLoader` forces off in production. Never available in production. |
| POST/GET | `/api/maintenance/tests/run` | maintenance, tests enabled only | Runs the CFML test suite; optional `?filter=Name`, and `?part=<n>&of=<m>` to run one deterministic slice (specs are dealt out round-robin over a stable name order, so every spec runs in exactly one part). 400 `INVALID_TEST_PARTITION` when `part` is outside 1..`of`. |

### Walk mutation and concurrency envelope

`clientMutationId` is **required** on create, save, complete, and void. `rowVersion` is **required**
on save, complete, and void; create does not take one because there is no prior row. Both are
validated before anything is written, so a missing or malformed token is a 400 that leaves the walk
and its row version untouched.

`clientMutationId` is a client-generated GUID recorded with the committed outcome in
`icf.walk_mutation` inside the same transaction. Each record is bound to its **actor**, **action**,
**target**, and a **SHA-256 fingerprint of the canonical semantic request** (migration
`004_mutation_fingerprint.sql`). The fingerprint covers the action, the target (the walk, or the org
unit for a create) and the meaningful body fields -- the whole-state `dimensions`/`responses` for
create and save, the `reason` for a void. It deliberately excludes `rowVersion`, `clientMutationId`,
`changedAt`, and the requested `versionId`: those are not what the request means, and a create
retried after a newer instrument version is published must still match what it committed.

A replay is answered by five checks in a fixed order, each a precondition of the next:

1. **Authorization.** Every replay re-authorizes the recorded walk against the current principal and
   the current authorization graph before anything about it is returned: a mutation id recorded
   before the caller's access changed answers 404, exactly as opening that walk would, and never
   discloses it.
2. **Actor, action, target.** A mismatch is 409 `MUTATION_ID_REUSED`.
3. **Provenance.** A row written before migration `004` carries no fingerprint, so nothing recorded
   can prove this request is the request it committed. It is never replayed as a success: 409
   `MUTATION_LEGACY_UNVERIFIABLE` with `details { walkId, clientMutationId, recordedAt }`, no
   application state written, and an exact-looking retry and an altered one get the same answer
   because they are indistinguishable against a NULL fingerprint. Reload the walk and retry under a
   new mutation id. Fingerprints are never fabricated or backfilled.
4. **Semantic request.** A different fingerprint under the same id is 409 `MUTATION_ID_REUSED`.
5. **Coherence.** A replay returns the recorded walk as it stands now, while the retrying client
   still holds the state that went with the *original* mutation. The recorded outcome is replayed
   (`walk.replayed = true`, `walk.mutation` = the recorded result, nothing written) only while the
   row version that mutation committed is still the row version in the database. If the aggregate has
   advanced since -- another session saved, completed, or voided the walk -- the replay is 409
   `MUTATION_REPLAY_SUPERSEDED` with
   `details { walkId, clientMutationId, recordedRowVersion, recordedAt, status }`: the mutation did
   commit, and the client must reload and reconcile. The details carry the **recorded** row version,
   never the current one, so a stale local state is never handed a token that would let it overwrite
   newer work. An idempotent replay therefore never pairs stale client state with a row version
   representing newer server state.

Browsers must therefore treat as **ambiguous** -- the mutation may already have committed -- a
transport failure, any HTTP 5xx, and *any answer they cannot use*: a 200 whose body is not JSON, a
200 whose JSON is not the expected document, a body that fails after its headers arrived, or any
other unexpected failure once the request has left. Only a definitive server answer says for certain
that nothing was committed. Every ambiguous outcome is retried with the same `clientMutationId` and
the **same semantic body**, held in an immutable per-operation record rather than rebuilt from the
UI as it now stands (docs/DATA_CONTRACT.md, "Pending mutation operations in the browser"). An operation id
is spent only on a definitive success or a definitive, non-retryable 4xx --
`MUTATION_REPLAY_SUPERSEDED` and `MUTATION_LEGACY_UNVERIFIABLE` among them, both of which mean
reload and reconcile.

`rowVersion` is the SQL Server rowversion of the walk row as a hex token (`0x` + 16 hex digits) and
must match the current row inside the transaction; the walk row is locked (`UPDLOCK`) while the
aggregate is written, so concurrent saves of one walk serialize and the later one is told it is
stale. Rows recorded before migration 004 carry no fingerprint and are never replayed as a success
(check 3 above).

### Part 4 teacher email draft (Phase 5)

The draft is **not** an endpoint. It is one ordinary response of the walk (`email_workflow`, type
`EMAIL_DRAFT_JSON`) and travels on `PUT /api/walks/{id}` with every other answer, so it inherits the
whole Phase 4 envelope: CSRF, the row-version compare, mutation idempotency and fingerprinting,
replay coherence, revisions, and the conflict panel.

`WalkPayloadValidator.validateEmailDraft` accepts only
`{ includedPartKeys: string[], drafted: boolean, to, subject, body: strings }` and stores the
document as canonical JSON with its keys in the order `body, drafted, includedPartKeys, subject, to`
(400 `INVALID_EMAIL_DRAFT` for anything else; `walk_response.text_value`, so the 20,000-character
`VALUE_TOO_LONG` limit applies). The browser serializes in that same order
(`app/assets/js/email-composer.js`), so a reload compares equal and no phantom unsent edit appears.

`to` is optional free text, never validated as an address and never parsed as a mail header: the
server does not send mail. **There is no mail endpoint and no mail delivery anywhere in the
application** (`docs/OPEN_DECISIONS.md`). The browser's only outbound action is a `mailto:` URL whose
`to`, `subject`, and `body` are percent-encoded, so a CR/LF or a `bcc:` in the recipient is data in a
URL and can never become a header. `POST /api/walks/{id}/email`, `/send`, and `/notify` do not exist
and answer 404; `tests/node/no-mail.test.mjs` is the standing gate.

### Server-authoritative state on a whole-state save

A save carries the whole working state, and the server owns what that means:

- visibility and applicability are derived from the walk's **pinned** instrument, never from the
  browser;
- a value the instrument currently hides is taken from the database: an omitted hidden value is
  **retained** without the browser echoing it, and a value a client sends for a hidden target is
  ignored (`SAVE` still answers 200; the response `state` shows the stored value);
- a value that was hidden and is now visible reappears from the database when the browser omits it;
- a **visible** key the browser omits is a cleared value -- that is how CLEAR works;
- a skippable component marked No clears its rating responses to `NOT_APPLICABLE` and keeps its
  notes, whatever the browser sends; switching back to Yes returns them as `UNANSWERED`;
- a walk at a SCHOOL org unit carries the School dimension value its unit is **mapped** to
  (`icf.org_unit_dimension_map`, migration `005`; never a comparison of `org_unit_code` with a value
  code). The server fills and locks that value and refuses any other, including "Other", with 409
  `SCHOOL_ORG_MISMATCH`. A unit with no validated mapping for the walk's pinned version fails closed:
  nothing is filled and any submitted School value is 409 `SCHOOL_ORG_UNMAPPED`. Both refusals write
  nothing and leave the row version where it was;
- `observed_at` follows the Visit Date dimension and falls back to the walk's immutable creation
  instant when no Visit Date is present -- including after one is cleared.

Import result shape (instrument import):

```json
{
  "instrumentId": "GUID", "versionId": "GUID", "instrumentCode": "ICFWALK",
  "versionLabel": "2026-09-17 aligned prototype", "status": "DRAFT", "created": true,
  "checksum": "sha256 of the stored canonical snapshot",
  "definitionsChecksum": "sha256 of the definitions block",
  "snapshotFormat": "icfwalk-instrument-snapshot/1",
  "counts": { "sections": 23, "items": 144, "responseSets": 29, "responseOptions": 138, "rules": 12, "dimensions": 10, "dimensionValues": 95, "instrumentDimensions": 10, "placeholders": 17 },
  "warnings": [ { "code": "PLACEHOLDER_CONTENT", "message": "...", "path": "..." } ],
  "placeholders": [ { "itemKey": "prek_k_q1", "sectionKey": "prek_k_classroom", "sourceLocation": "PREK_K_ITEMS[0]", "reviewStatus": "Placeholder in source" } ],
  "elapsedMs": 1234, "sourcePath": "instrument-config.json"
}
```
