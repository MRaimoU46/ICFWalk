# Acceptance test tracking

Keyed to `docs/ACCEPTANCE_TESTS.md`. Status values: **PASS** (automated evidence in this
repository), **PASS (manual)**, **NOT TESTABLE HERE** (needs the real Adobe ColdFusion 2023 /
SQL Server deployment or a later phase), **PENDING** (later phase), **N/A** (with reason).

Verification runtime note: Adobe ColdFusion 2023 could not be installed in the build environment.
CFML evidence was produced on Lucee 6.2.8 (Jetty) against Microsoft SQL Server 2022 Developer in
Docker. Every CFML-backed PASS below must be re-run on Adobe ColdFusion 2023 before handoff; the
commands are identical (`npm test`). Items whose behavior depends on Adobe-specific semantics are
marked explicitly.

**Current status (branch `claude/icfwalk-phase-6-7-integration`):** a Phase 6 and Phase 7 integration
candidate, awaiting a focused independent integration audit. Its merge commit
`68f9026d39ba0ff44d12d6398c5e933971dad2f4` passed its exact-commit gate: Node/HTTP/Playwright 245/245, CFML
517/517, 0 failed, 0 skipped (`docs/evidence/phase6-7-integration-handoff.md`). Phase 6 is accepted and frozen at code
commit `158debca5da2c4f3a07f602689cd08af9db9bd6e`, for the verified scope: the independent re-audit
passed and P6A-R01 is closed (`docs/evidence/phase6-freeze.md`, "Acceptance"). That acceptance
covers `158debc`, not this merge. The Phase 7 corrections merged here
(`e0342143074f727475ae2d4cb6933fa279902f85`) await independent re-audit: Phase 7 is not frozen and
not accepted, and its findings keep the status its ledger gives them. Not production-certified:
Adobe ColdFusion 2023, SQL Server 2016, connector-level limits, Microsoft Excel and a screen reader
have not been used.
Sections below that describe earlier rounds keep the status they had when they were written.

## Package and configuration

| ID | Status | Method / evidence |
| --- | --- | --- |
| PKG-01 | PASS | `node scripts/validate-handoff.mjs` exit 0, 51 checks (`tests/node/package.test.mjs`). Validator patched to ignore `.git/` and report build files (Phase 0 defect, recorded in `BUILD_STATUS.md`). |
| PKG-02 | PASS | `tests/node/package.test.mjs`; CFML validator rule `RETIRED_CONTENT_PRESENT` (`InstrumentConfigValidatorTest`). |
| PKG-03 | PASS | `tests/node/package.test.mjs` (17 = 3/3/3/3/5); import result reports 17 placeholders (`InstrumentImportServiceTest.testDb04...`). |
| PKG-04 | PASS | `tests/node/package.test.mjs`. |
| PKG-05 | PASS | `tests/node/package.test.mjs` reads the aligned workbook sheets (23/144/29/138/12/10/95/10). |

## Database and seed

| ID | Status | Method / evidence |
| --- | --- | --- |
| DB-01 | PASS | `tests/node/db-scripts.test.mjs` on SQL Server 2022 (16.0.4295): both scripts commit; 20 tables; 5 roles; instrument ICFWALK. |
| DB-02 | PASS | Same test: rerun of 001 raises error 50001, table and role counts unchanged. |
| DB-03 | PASS | Same test: 002 re-applied without error, one `definition` column. Phase 4 migration `003_walk_mutation.sql` and correction migration `004_mutation_fingerprint.sql` applied and re-applied without error in the same test (21 tables, 7 `walk_mutation` columns, one `request_fingerprint` column, digest constraint accepts lower-case hex and refuses anything else, pre-patch rows stay valid). |
| DB-04 | PASS | `InstrumentImportServiceTest.testDb04ImportCreatesDraftWithMappedGuidsAndSnapshot` (GUID mapping, snapshot stored, counts 23/144/29/138/12/10, no orphans, audit event). |
| DB-05 | PASS | `...testDb05ReimportIsIdempotentWithStableGuidsAndChecksum` (same version id, identical checksum, no duplicate rows, GUIDs reused) and `...testReorderedReimportSucceedsAndReturnsToGolden`. Seed endpoint idempotency in `tests/node/cfml-suite.test.mjs`. |
| DB-06 | PASS | `...testDb06ImportAgainstPublishedVersionIsRefusedWithoutChanges` (409 `INSTRUMENT_VERSION_IMMUTABLE`; version and item row versions unchanged). |
| DB-07 | PASS | `...testDb07MissingResponseSetRollsBackWithSpecificError` (`MISSING_REFERENCE` naming `rs_scale_comp_s1_q1`, no version row). |
| DB-08 | PASS | `...testDb08CorruptConditionsJsonRollsBack` (`INVALID_JSON`, no partial version) plus `...testTransactionRollsBackWhenDatabaseRejectsAWrite`. |
| DB-09 | PASS | `...testDb09ResponseOptionDefinitionsMatchTheJsonExactly` (138 options, 136 definitions byte-equal; DB round-trip definitions checksum equals the golden). |

## Authentication and authorization

| ID | Status | Method / evidence |
| --- | --- | --- |
| AUTH-01 | PASS | `tests/node/auth.test.mjs`: `/api/me`, `/api/auth/csrf-token`, `/api/admin/instrument/versions`, and `POST /api/auth/sign-out` return 401 `UNAUTHENTICATED` with no protected data; SSO gateway headers sent directly are ignored. Phase 7: `/api/reports/options`, `/api/reports/aggregate` and `/api/reports/aggregate.csv` answer 401 `UNAUTHENTICATED` with a body of `error` only (`reports.test.mjs` "AUTH-01"). |
| AUTH-02 | PASS | `ConfigLoaderTest` (startup refused with `CONFIGURATION_INVALID`), `IdentityTest.testDevelopmentStubCannotBeConstructedWhenNotPermitted` (factory and constructor refuse), `IdentityTest.testConfigLoaderRejectsUnsafeIdentitySettings`. |
| AUTH-03 | PASS | `AuthorizationTest.testAuth03...`: district walk/report role reads walks in descendant schools, edits its own, creates in scope. `auth.test.mjs`: `/api/me` permissions for the district walker. |
| AUTH-04 | PASS | `AuthorizationTest.testAuth04...`: school role denied (404, audited) for walks in unassigned schools even with the GUID. |
| AUTH-05 | PASS | `AuthorizationTest.testAuth05...` (service), `WalkServiceTest.testCrossScopeAccessFailsClosedThroughTheService`, and `tests/node/walks.test.mjs` "AUTH-05 / AUTH-06": report-only and instrument-admin roles get 403 `FORBIDDEN` on list, create, open, instrument, save, complete, void, and delete, with no walk identifier, note, or teacher field in the response. Phase 5 extends this to the summary export: `walks.test.mjs` "AUTH-04/05" and `WalkServiceTest.testSummaryAuthorizationMatchesOpen` -- 401 without identity, 403 for report-only, instrument-admin, and a user with no walk capability, 404 for another school (the same answer `open` gives, so existence is not disclosed), 400 for a malformed id; every refusal body is checked to contain no summary text. |
| AUTH-06 | PASS | `AuthorizationTest.testAuth06...` (admin has no walk/report capability; walkers lack `instrument.manage`) and `auth.test.mjs` (admin route 403 for walkers and role-less users, 200 for the admin; admin `/api/me` shows no walk/report scope). Phase 7: the same separation holds for reports -- an instrument administrator and a role-less user get 403 from every report route and service entry point (`ReportServiceTest.testInstrumentAdminAndRolelessUsersAreRefused`, `reports.test.mjs` "AUTH-06 / RPT-03"). |
| AUTH-07 | PASS | `AuthorizationTest.testAuth07...` (future and expired assignments yield no scope) and `testEndingAnAssignmentRevokesAccessImmediately`. |
| AUTH-08 | PASS | `AuthorizationTest.testAuth08...`: descendants included, unrelated branch and inactive descendants excluded; `include_descendants = 0` covers only the assigned unit. |
| AUTH-09 | PASS | Org-unit and walk identifiers: `AuthorizationTest.testAuth09...`. Version, walk, item, option, dimension, and value tampering through the walk endpoints: `WalkServiceTest.testAuth09TamperedKeysCodesAndIdentifiersAreRejectedAndAudited` (15 payload cases + version/walk/row-version/mutation-id/malformed/unknown ids; nothing written, row version unchanged, each rejection audited `WALK_SAVE_REJECTED`) and `walks.test.mjs` "AUTH-04 / AUTH-09" over HTTP (10 payload cases, 404 for other schools, 403 for a same-school colleague). Item/option/dimension GUIDs are never accepted from the client: keys and codes are resolved to the pinned version's GUIDs on the server. |

## My Walks and lifecycle

Phase 4 re-verified every row end to end against SQL Server through the service
(`WalkServiceTest`), HTTP (`walks.test.mjs`), and the browser (`browser.test.mjs`,
`browser-persistence.test.mjs`).

| ID | Status | Method / evidence |
| --- | --- | --- |
| WALK-01 | PASS | `browser.test.mjs` "WALK-01" against the server list: empty-state wording and the `+ New walk` action; `browser-persistence.test.mjs` shows the empty state again after the last walk is voided. |
| WALK-02 | PASS | `WalkServiceTest.testWalk02CreatePinsCurrentVersionOwnerAndDefaults` (DRAFT, pinned `version_id` = current version, owner, org unit, defaults, one response row per response-capable item, audit `WALK_CREATED`); `walks.test.mjs` "WALK-02 / WALK-03"; `browser-persistence.test.mjs` "WALK-02 / WALK-05" (reload shows the persisted walk). |
| WALK-03 | PASS | `WalkServiceTest.testWalk03CreateRetryWithSameMutationIdCreatesOneWalk` (same id → same walk, `replayed`, one mutation row, other user refused `MUTATION_ID_REUSED`); `walks.test.mjs` "WALK-02 / WALK-03". |
| WALK-04 | PASS | `WalkServiceTest.testWalk04ListSortsByUpdatedAtDescendingWithCardDimensions` (server order by `updated_at`, card dimensions in the list DTO); `browser.test.mjs` "My Walks" (grade · content, school, date, relative time, newest first). |
| WALK-05 | PASS | `WalkServiceTest.testSaveRoundTripPersistsTypedValuesAndSurvivesRetrieval` (typed columns, Other mapping, date, notes, email draft re-read from the database); `walks.test.mjs` "WALK-05 / SAVE-08 / SEC-01"; `browser-persistence.test.mjs` "WALK-02 / WALK-05" (values restored after a page reload). |
| WALK-06 | PASS | Drafts: `browser.test.mjs` "My Walks" (confirmation wording, card removed) → `POST /void` with the default reason, audit `WALK_VOIDED` (`WalkServiceTest.testWalk06And08VoidLifecycleAndDeleteRefusal`). Completed walks: reason required, voided with reason, rows retained (`browser-persistence.test.mjs` "WALK-06 / WALK-08"). |
| WALK-07 | PASS | `browser.test.mjs` "My Walks": cancel leaves the list unchanged (no request is made). |
| WALK-08 | PASS | `DELETE /api/walks/{id}` is always 409 `WALK_DELETE_REFUSED` and audited; completed walks are voided with a reason and keep every row (`WalkServiceTest.testWalk06And08...`, `walks.test.mjs`). |
| WALK-09 | PASS | `WalkServiceTest.testWalk09CompletionRejectsMissingRequiredResponsesAndKeepsTheDraft` (7 field-specific errors with key/section/message, DRAFT and values untouched, audit `WALK_COMPLETION_REJECTED`); `browser-persistence.test.mjs` "WALK-09 / WALK-10 / A11Y-02" (alert summary, links focus the field, `aria-invalid` + description, errors clear as answered). |
| WALK-10 | PASS | `WalkServiceTest.testWalk10CompletionSetsStatusTimestampRevisionAndAudit` (COMPLETED, `completed_at`, revision 1 `COMPLETE` with the prior snapshot, audit `WALK_COMPLETED`, idempotent retry, second completion refused); post-completion edits append `POST_COMPLETION_EDIT` and must stay complete (`testPostCompletionEditAppendsARevisionAndMustStayComplete`); browser completion and badge. |
| WALK-11 | PASS | `WalkServiceTest.testWalk11OlderWalkRendersFromItsPinnedSnapshotAfterANewVersion`: a newer DRAFT with a changed prompt becomes the version for new walks while the older walk keeps its `version_id`, renders the original prompt through `GET /api/walks/{id}/instrument`, and still saves against its own definitions; `walks.test.mjs` "WALK-11". Publishing itself is Phase 6. |

## Metadata and conditional UI

Each COND row is proven three ways: `VisibilityEngineTest` (CFML engine), `tests/node/visibility.test.mjs`
(browser engine against the served model, plus the shared vectors), and `tests/node/browser.test.mjs`
(real DOM in Chromium). Phase 4 adds the persisted states (`WalkServiceTest`).

| ID | Status | Method / evidence |
| --- | --- | --- |
| COND-01 | PASS | Elementary school → `prek,k,1,2,3,4,5` (engine tests and DOM `<select>` options). |
| COND-02 | PASS | Middle school → `6,7,8`. |
| COND-03 | PASS | High school, Dream Academy, Central School → `9,10,11,12` (from `valueGroup` metadata, not names). |
| COND-04 | PASS | Other → full PreK–12 list; no school → full list. |
| COND-05 | PASS | Grade 5 + middle school → grade cleared (`DIMENSION_CLEARED/OPTION_FILTER`), PreK–K section hides with the cleared grade. Server-side on save: `WalkServiceTest.testCond05And06GradeFilterClearsAndHiddenPeriodIsRetained` (grade row deleted). |
| COND-06 | PASS | Period visible for 6–12 only; hidden value retained as `HIDDEN` (`ICFWALK_HIDDEN_PERIOD_POLICY=RETAIN_HIDDEN`, `CLEAR` also tested). Persisted: the Period row is retained and `dimensionStates.period = HIDDEN` (`WalkServiceTest.testCond05And06...`). |
| COND-07 | PASS | PreK and K show PreK–K Classroom; every other grade hides it. |
| COND-08 | PASS | Dual Language, MAC, PREP, Ignite, AVID, ESL each show only their section; General Education shows none; Other free text never matches. |
| COND-09 | PASS | Art, Music, CTE show Content-Area Look-Fors; other content areas hide it. |
| COND-10 | PASS | Engine + DOM (Phase 3) and persisted: `WalkServiceTest.testCond10HiddenSectionAnswersArePersistedAsHiddenAndReturn` (row state `HIDDEN` with the option retained, `ANSWERED` again when the class type returns). |
| COND-11 | PASS | New walk: both applicability controls pressed `No`, rating rows hidden, header reads `Not part of this lesson`. |
| COND-12 | PASS | Yes → rate → No in the DOM (Phase 3) and on the server in one transaction: `WalkServiceTest.testCond12And13SkippableComponentClearsRatingsKeepsNotesInOneTransaction` (both ratings `NOT_APPLICABLE` with NULL options, notes `ANSWERED`, `changes[]` returned), `walks.test.mjs` "COND-12 persisted". |
| COND-13 | PASS | Back to Yes: rows return `UNANSWERED`, cleared ratings do not reappear, notes remain (DOM and persisted rows, same tests as COND-12). |
| COND-14 | PASS | `RenderModelTest` compares all 138 options and definitions with the JSON; `browser.test.mjs` opens every visible definition toggle and compares the rendered rows. |
| COND-15 | PASS | Counts use answered ratings only (`1/2 rated`); an option code outside the set is `UNANSWERED`; blanks never become zero. |

## Autosave, summary, administration, reporting

| IDs | Status |
| --- | --- |
| SAVE-01 | PASS. `browser-persistence.test.mjs` "SAVE-01 / SAVE-02": four edits within 700 ms show `Unsaved changes` immediately and produce exactly one `PUT` after activity stops; a later edit is a second save with a new mutation id and the committed row version. |
| SAVE-02 | PASS. Same test: `Saving...` → `All changes saved`, row version advanced on the server; `WalkServiceTest.testSaveRoundTrip...` (new `rowVersion` on every save). |
| SAVE-03 | PASS. `browser-persistence.test.mjs` "SAVE-03": aborted `PUT` shows the network message with a Retry action, input stays on screen, nothing reached the server; Retry sends the same `clientMutationId` and succeeds; a server 500 shows its code and retries the same way. |
| SAVE-04 | PASS. `WalkServiceTest.testSave04StaleWriteIsRejectedWithoutOverwriting` (409 with the server row version, A's write intact, audit `WALK_SAVE_CONFLICT`), `walks.test.mjs` "SAVE-04 / SAVE-06", `browser-persistence.test.mjs` "SAVE-04 / SAVE-05" (two browser contexts). |
| SAVE-05 | PASS. Browser: the stale session gets an `alertdialog` listing only its unsent edits against the saved values; "Keep my edits and save" applies them on the server record and saves with the new row version (both sessions' values end up stored); "Use the saved version" discards them. |
| SAVE-06 | PASS. `WalkServiceTest.testSave06RetryWithSameMutationIdCommitsOneLogicalChange` (replay returns the committed row version, no duplicate response/dimension/revision rows, one CREATE + one SAVE mutation, later replay still replays, foreign reuse refused); HTTP equivalent in `walks.test.mjs`. |
| SAVE-07 | PASS. `WalkServiceTest.testAuth09...` and `walks.test.mjs`: `yes` on a 1–5 item, `1` on a No/Partial/Yes item, `4` on a yes/no item are 400 `INVALID_OPTION`; codes are compared exactly (the CFML `==` coercion of `yes`/`1` was found and fixed; `visibility-vectors.json` carries three vectors for it). |
| SAVE-08 | PASS. Markup and SQL metacharacters in notes and Other text are stored verbatim and returned as JSON data (`WalkServiceTest.testSaveRoundTrip...`, `walks.test.mjs` with `nosniff`); in the browser the text is shown in the textarea, no element is injected, no script runs (`browser-persistence.test.mjs` "WALK-02 / WALK-05 / SAVE-08"). Export preview, admin, and reports: later phases. |
| SUM-01 | PASS. One formatting contract implemented twice and proven byte-identical: `WalkSummaryFormatterTest.testMatchesTheSharedSummaryVectorsExactly` and `summary.test.mjs` compare every vector in `tests/fixtures/summary-vectors.json` byte for byte (CFML uses `compare()`, never `==`). The persisted path is proven separately: `WalkServiceTest.testServiceSummaryMatchesTheFormatterForAPersistedWalk` (walk saved through `WalkService.save`, then exported), `walks.test.mjs` "SUM-01/05" (the HTTP body equals the vector text), and `browser-email.test.mjs` "SUM-01" (the downloaded file equals the route's bytes and the vector). Every vector was reviewed against `source/current-prototype.html` with `scripts/prototype-summary-oracle.mjs`; the only differences are the four deviations recorded in BUILD_STATUS. |
| SUM-02 | PASS. Blanks print `not answered` and never a zero: `summary.test.mjs` / `WalkSummaryFormatterTest.testComponentAverageUsesAnsweredScoresOnly` (4 and a blank average 4.0, not 2.0; nothing answered is `n/a`, never `0.0`; a foreign option code is not an answer), `walks.test.mjs` "SUM-02/03/04" over HTTP, `browser-email.test.mjs` "SUM-02/05" on the downloaded file. |
| SUM-03 | PASS. A skipped component prints `2.3 WORKSHOP MODEL OF INSTRUCTION  (not part of this lesson at the time of the visit)` with no average and no rating lines, and keeps its retained notes: the `workshop model no with notes` vector, plus explicit cases in both formatter suites, `walks.test.mjs`, and the CFML service test. |
| SUM-04 | PASS. A hidden section and a hidden Period are excluded from the text, the file name, and the email, and are not deleted: `WalkSummaryFormatterTest.testHiddenSectionsAndHiddenPeriodAreExcluded` and `summary.test.mjs` (the same values print again once the instrument shows them), `walks.test.mjs` "SUM-02/03/04" (answered while visible, then hidden; `GET /api/walks/{id}` still returns them as `HIDDEN`). |
| SUM-05 | PASS. Only `[A-Za-z0-9_-]` survives in the label, runs collapse to one underscore, case is kept, and an unnamed walk falls back to its id: `sanitizeFileLabel` vectors in both suites (`Unlisted/Site?` → `Unlisted_Site_`, `../../etc/passwd` → `_etc_passwd`), `testFileNameSanitization`, `summary.test.mjs` SUM-05 (four traversal/quoting attempts), `walks.test.mjs` "SUM-05" on the real `Content-Disposition` header (one filename parameter, nothing that could close the quoted string), and `browser-email.test.mjs` on the downloaded file name. |
| SUM-06 | PASS. Subject and body match the prototype's wording exactly (the oracle found no deviation at all in any generated draft), only checked parts appear, and the settings order wins over the tick order: the `emails` block of every vector in both suites, `WalkSummaryFormatterTest.testEmailDraftPartsAndTemplates`, `summary.test.mjs` SUM-06, and `browser-email.test.mjs` "SUM-06" (the composer's output equals the shared formatter's output for the walk as the server holds it). |
| SUM-07 | PASS. `browser-email.test.mjs` "SUM-07": To/Subject/Body edited, autosaved, page reloaded, walk reopened, all three restored exactly, the ticked parts restored, and no conflict panel (the canonical key order is what makes the reload compare equal). API level: `WalkServiceTest.testEmailDraftRoundTripAndSchema` and `walks.test.mjs` "SUM-07/09". Non-reportable by contract: `walk_response.text_value` of a non-reportable item, excluded from Phase 7. |
| SUM-08 | PASS. `browser-email.test.mjs` "SUM-08": the clipboard holds `Subject: <subject>\n\n<body>`, the button reads `Copied!`, the `mailto:` URL is percent-encoded, the page does not navigate, and no request to any mail route is made. `no-mail.test.mjs` pins the encoding on the function itself (CR/LF → `%0D%0A`, `bcc:` → `bcc%3A`, never a `bcc=` parameter) and pins that the button's only action is `window.location.href = mailtoUrl(...)`. No server-side send exists: see the gate row below. |
| SUM-09 | PASS. `browser-email.test.mjs` "SUM-09": Clear hides the box, empties subject and body, sets `drafted:false`, keeps the recipient and the ticked parts, and leaves every other response and dimension byte-identical (compared before/after minus `email_workflow`). API level: `WalkServiceTest.testEmailDraftRoundTripAndSchema` and `walks.test.mjs` "SUM-07/09". |
| No automatic send (Phase 5 gate) | PASS. `no-mail.test.mjs`: no `cfmail`, SMTP, or mail library anywhere in the shipped source; the composer issues no request of any kind and names no endpoint; the walk routes are exactly the Phase 4 set plus the read-only summary export; `.env.example` and `ConfigLoader` configure no mail transport; `POST /api/walks/{id}/email`, `/send`, `/notify`, `/api/mail`, `/api/email` all answer 404. Also asserted from the walk side in `walks.test.mjs` "SUM-08". |
| ADM-01 | PASS | **Phase 6.** The administrator's import is `POST /api/admin/instrument/import` (`instrument.manage` + CSRF) and the Import card of the administration view. `InstrumentAdministrationTest.testImportOfTheSuppliedDocumentReturnsTheValidationSummary`: the supplied document imports into a DRAFT and the result carries every count (23 sections, 144 items, 29 response sets, 138 options, 12 rules, 10 dimensions, 95 values) and the 17 placeholder warnings and placeholders. `admin-instrument.test.mjs`: the same over HTTP with the admin's user id as the audited actor, a re-import without a replacement refused 409 `DRAFT_REPLACEMENT_REQUIRED` and with the exact replacement 200 `created: false` (audit correction P6A-02, below), an invalid document 422 with every issue and nothing written, the body contract (`IMPORT_BODY_INVALID`, `DOCUMENT_REQUIRED`) and the 413 size cap; every admin route refuses anonymous (401), non-`instrument.manage` (403) and missing or wrong CSRF (403). `browser-admin.test.mjs`: an admin-only user lands on the administration view without calling a walk or report route, uploads the document from a file and sees the summary (counts table, 17 placeholder prompts), an invalid file shows its issues and writes nothing, and a non-JSON file is refused on the page. |
| ADM-03 | PASS at the service and endpoint level. **Phase 6 administration UI:** Publish is in the version list behind a confirmation, and any refusal is shown with the server's code and its `details.issues` list (the same rendering the import's invalid-document path uses, which is browser-tested). A 422 publish refusal specifically is **not** browser-tested: producing one needs a checksum-matching invalid DRAFT, which only the CFML harness builds. The service and HTTP evidence below is unchanged. Publishing now refuses a DRAFT whose content is invalid, not only one whose parts disagree. `DefinitionValidator` is the one authoritative semantic rule set over normalized definitions; `InstrumentConfigValidator` runs it on every imported document and `InstrumentPublishService` runs it, under the version's row lock, on the stored snapshot's definitions and on the definitions SQL Server holds, each in its own right, plus the snapshot envelope and its claimed identity. Executed: `InstrumentPublishServiceTest`'s fifteen `testSemantic…` cases, each of which builds a **checksum-matching, drift-free** invalid version (definitions edited, snapshot recompiled from them, checksum recomputed; the helper asserts both preconditions before publishing) and asserts 422 `INSTRUMENT_VERSION_NOT_PUBLISHABLE`, still DRAFT, no publisher, no success audit, exactly one refusal audit. Classes covered that way: structurally incomplete definitions, broken section hierarchy, choice item without a response set, response set emptied, option in a foreign set, invalid option order, malformed conditions document, unsupported rule effect, rule target missing, invalid dimension data type and blank dimension label, invalid dimension-value order, placement rule reference broken, snapshot identity mismatch, counts mismatch, unsupported snapshot format. Where a SQL constraint makes a class unpersistable — every logical key is unique-constrained and case-insensitive, and `FK_section_parent_same_version` / `CK_section_not_self_parent` forbid a bad parent — that is proved separately (`testDuplicateLogicalKeysCannotBePersistedAtAll`) and the rule itself is proved at the validator's own boundary (`DefinitionValidatorTest`, 45 cases). Over HTTP: `admin-publish.test.mjs` "a checksum-matching semantically invalid DRAFT is refused…" asserts the established `details.issues[] = { code, message, path }` response and that nothing changed. Red-before-green in `docs/evidence/phase6-correction-red-before-fix.md`: with the semantic refusals removed, all fifteen versions publish. **Second correction:** the rule set now also covers every state the runtime renderer rejects or renders incompletely, and publication additionally builds the render model from the exact snapshot it is about to freeze (`RenderContractValidator`), so a renderer failure becomes a structured `{ code, message, path }` refusal rather than a runtime 500 and a build that silently drops a subtree is refused as `RENDER_MODEL_INCOMPLETE`. Eleven further `testSemantic…` cases cover those states on the same checksum-matching, drift-free basis (no active root, two active roots, section and item orphaned from the root, `MULTI_CHOICE`, `SHORT_TEXT`, unknown option filter, inactive response set, choice item with no active option, placement of an inactive dimension, list placement with no active values), and `ValidatorRendererContractTest` (18 cases) pairs each rule with the renderer behaviour it exists for and proves two representative valid instruments build completely. The snapshot envelope is now enforced in full -- all nine `counts` members, each a whole non-negative number equal to the definitions' own, no extra members -- and the documented canonical-byte invariant is enforced (`SNAPSHOT_NOT_CANONICAL`), with regressions for a missing block, `{}`, every missing member, non-numeric, fractional and negative values, an unexpected member, and noncanonical bytes carrying a recomputed matching checksum. `ImportPublishEquivalenceTest` (14 cases) proves import and publish report the same code at the same path for the same normalized invalid state, and that inbound-only failures stay import-specific. Red-before-green for all of it in `docs/evidence/phase6-second-correction-red-before-fix.md`. |
| ADM-04 | PASS. **Phase 6 administration UI:** Publish asks first (inline `alertdialog`, focus on Cancel, Escape cancels) and a cancelled publish changes nothing (`browser-admin.test.mjs` "ADM-04 / ADM-07 (browser)"). The browser sends no body (`api.postEmpty`), matching the route's no-body contract. **Freeze.** `InstrumentPublishServiceTest.testPublishFreezesTheImportedSnapshotByteForByte`: the frozen snapshot is byte-identical to the imported one, its checksum still hashes it, and `status`/`published_at`/`effective_start`/`checksum_sha256`/`compiled_snapshot_json`/`published_by_user_id` are all written by the one `markPublished` statement (`CK_instrument_version_publish_values` and `CK_instrument_version_publisher_required` reject any non-DRAFT row missing one). `testAPublishedVersionBecomesSelectableAsCurrent` proves the published row satisfies the current-version selection predicate. **Publisher attribution** is proved, not assumed: `testPublishRecordsTheAuthenticatedPublisher` (row, result and success audit all name the same user), `testPublishingWithoutAValidPublisherIsRefusedAndChangesNothing` (missing, blank, malformed and unknown publishers each refused with nothing written), `testMarkPublishedRefusesAnEmptyPublisher` (no lower-level bypass). **Live HTTP** (`admin-publish.test.mjs`, 14 cases against the running application and real SQL Server): anonymous 401; non-`instrument.manage` 403; missing and invalid CSRF 403; malformed id 400; unknown id 404 without disclosure or mutation; the no-body contract enforced on the raw request rather than on the parsed struct, so no body, `{}`, `{ }`, whitespace only, a newline only, `null`, `[]` and a body naming a publisher each answer 400 with a stable code and move no row version, and a body naming another actor or publisher cannot override the session (a JSON `null` body is 400 `INVALID_JSON_BODY`, not a 500); successful publication with the publisher taken from the authenticated principal; repeat publication a deterministic 409 with no second success event; a lost-response retry unable to publish twice or move the frozen row; two concurrent publishes producing exactly one transition and one refusal with one success audit; and publish-versus-mutation races resolving in one serial order without a deadlock. Those `Promise.all` races are supplemental stress coverage: they cannot show that two transactions ever overlapped. **Deterministic concurrency evidence** is a two-sided barrier (`tests/cfml/support/ConcurrencyBarrier.cfc`, with the test-only `armBefore` / `armAfter` seams of `tests/cfml/support/InterceptingDefinitionRepository.cfc`). The overlap is established by two independently observed signals, not inferred: transaction A emits `A_LOCKED` from inside its transaction once it holds the production lock and has done nothing else, and request B emits `B_AT_COMPETING_BOUNDARY` immediately before the database call that contends for that lock. The spec blocks on a `LinkedBlockingQueue` poll (no CFML sleep), releases A only after hearing B, asserts that both signals were observed and `signalledInOrder("A_LOCKED", "B_AT_COMPETING_BOUNDARY")`, and then asserts the one permitted serial outcome and the final state. That B had not completed while A held the lock is also asserted, but only as supplemental evidence after its arrival was independently observed; non-completion alone proves nothing about arrival. Seven deterministic contention pairings: publish versus publish (`PublishConcurrencyBarrierTest.testPublishVersusPublishReachesOneSerialOutcome`), publish versus import (`testPublishVersusImportReachesOneSerialOutcome`), import versus publish (`testImportVersusPublishQueuesRatherThanDeadlocking`), publish versus new dimension identity (`testPublishVersusNewDimensionIdentityReachesOneSerialOutcome`), publish versus new dimension-value identity (`testPublishVersusNewDimensionValueIdentityReachesOneSerialOutcome`), metadata versus metadata (`SharedMetadataConcurrencyBarrierTest.testADeactivationIsNotLostByAConcurrentRename`, `testARenameIsNotLostByAConcurrentDeactivation` and `testEachAuditDescribesTheRowItActuallyReplaced`, all through the same barrier), and import's shared-state check versus an authorized metadata update (`SharedMetadataConcurrencyBarrierTest.testImportQueuesForTheSharedRowAndThenDecidesOnIt`). The interception components are test-only: nothing in `src/` references them, the container never holds them, and no route, environment flag or remotely activatable hook reaches them. History and red evidence: CORR6-04. |
| ADM-05 | PASS. **Phase 6:** the administration UI offers no edit for a PUBLISHED or RETIRED version, and the edit route refuses one (409) with the version's checksum and row version unchanged (`admin-instrument.test.mjs`, `InstrumentAdministrationTest.testAPublishedVersionCannotBeEdited`); retirement was added to the lifecycle inventory (`InstrumentImmutabilityTest.testTheLifecycleTransitionsEnforceTheirOwnContract`: `markRetired` changes status and `effective_end` only, and only for a PUBLISHED version). **Version content.** Immutability is structural rather than procedural. Every `DefinitionRepository` method that writes version content locks the owning `instrument_version` row (`UPDLOCK, ROWLOCK`, through `requireDraftVersion` / `requireDraftOwnerOf`) and refuses a non-DRAFT before any write, resolves the owner from the child row when handed only a child id, and carries `status = N'DRAFT'` in its own DML besides; there is no unchecked deletion path. Executed: `InstrumentImmutabilityTest` invokes **every** production mutator — snapshot replacement, `markPublished`, order parking, section insert/update/place/delete, response set and option upsert and delete, rule upsert and delete, item upsert and delete, placement upsert and delete, version-scoped dimension value replacement, and the cascade delete — against a PUBLISHED fixture and a RETIRED one, asserting the typed refusal and then comparing status, snapshot, checksum, publisher, the version `row_version`, the definitions checksum, every child table's high `row_version` and every child count with what they were (`testEveryMutatorIsRefusedForAPublishedVersion`, `…ForARetiredVersion`). `testEveryRepositoryMutatorIsCoveredHere` reads the repository's own metadata and fails if a mutator exists that the spec does not exercise, so the inventory cannot silently go stale. `testTheSameMutatorsStillWriteForADraft` proves the boundary is a boundary and not blanket refusal, and `testAChildOfAnotherVersionCannotBeWrittenThroughThisOne` proves the owner is resolved from the database. Also proved: `testPublishingAPublishedVersionIsRefusedAndAudited`, `testAssertDraftForWriteRefusesAPublishedVersion`, `testAPublishedVersionCannotBeReimported`. **Shared and global rows are in the same inventory**, each under the contract it actually has (`docs/evidence/phase6-third-correction-mutation-inventory.md`), and exercised against it: `testEverySharedOrGlobalMutatorEnforcesItsOwnContract`, `testARefusedGlobalIdentityCreationWritesNothing`, `testGlobalIdentityCreationStillWorksForADraft`, `testTheSharedAndGlobalInventoryNamesOnlyRealMethods`. **Global reporting identity.** `createDimensionIdentity` and `createDimensionValueIdentity` each take the requesting version id and issue one status-qualified `INSERT ... SELECT` whose source is the owning `icf.instrument_version` row under `UPDLOCK, ROWLOCK`, predicated on `status = N'DRAFT'`, with `OUTPUT INSERTED` returning the row actually inserted. The authority decision and the write are one atomic statement, so a caller that owns no transaction has no window between them; nothing eligible means nothing inserted and a typed non-DRAFT (or not-found) refusal rather than a GUID. `GlobalIdentityBoundaryTest` (6 cases): `testMintingIdentityIsQualifiedByItsOwnStatement` asserts that structure over the real production SQL, `testBothCreatorsWriteForADraftOutsideAnyTransaction`, `testAFrozenVersionCannotMintIdentityAndNothingMoves` (SHA-256 fingerprint of both global tables across PUBLISHED and RETIRED refusals), `testAnAbsentVersionCannotMintIdentity`, and `testWhenTheMintWinsExactlyOneIdentityAppearsAndPublishStillSucceeds` / `testWhenTheValueMintWinsExactlyOneIdentityAppearsAndPublishStillSucceeds`; the converse serial outcome, publish winning, is the two identity pairings in ADM-04. **Shared instrument metadata.** Import does not write the shared `icf.instrument` row after the instrument's birth. It re-reads the row under `lockInstrumentById` **after** its version lock and decides `SHARED_METADATA_CONFLICT` on that locked row. `updateInstrument` is gone; `updateInstrumentMetadata` performs only the UPDATE, on a row the caller already holds under `lockInstrumentByCode` / `lockInstrumentById` (`UPDLOCK, HOLDLOCK, ROWLOCK`), and its `userExists` call is an integrity check for the audit foreign key, not authorization. It is reached only by `InstrumentMetadataService.updateMetadata`, which requires the **current principal** (anything that is not a principal is `INSTRUMENT_METADATA_PRINCIPAL_REQUIRED`), asks the central `AuthorizationService` for global `instrument.manage` before any mutation, and derives the audit actor only from `principal.userId`: there is no actor argument. Its locked read, merge, update and audit are one transaction. A change of `name` or `description` is detected case-sensitively and `active` is compared as a boolean, so a capitalization-only edit is written, versioned and audited, while a patch with no material difference is an audit-free no-op. `InstrumentMetadataServiceTest` (17 cases): a known, active user holding a real `DISTRICT_WALK_REPORT` assignment is denied 403 and changes nothing (`testAKnownUserWithoutInstrumentManageIsDeniedAndChangesNothing`), and so is a user with no assignment (`testAKnownUserWithNoRoleAtAllIsDeniedAndChangesNothing`); a user with a real effective `MASTER_INSTRUMENT_ADMIN` assignment succeeds (`testAUserWithAnEffectiveInstrumentManageAssignmentSucceeds`); the stored and audited actor is the principal and a supplied `actorUserId` argument is not honoured (`testTheAuditActorIsTheAuthorizedPrincipalAndCannotBeOverridden`); a bare user id and an empty struct are refused (`testTheOperationRequiresAPrincipalRatherThanAnActorId`); malformed patches are refused before any write; `testACaseOnlyNameChangeIsMaterial` and `testACaseOnlyDescriptionChangeIsMaterial` prove a capitalization-only change is stored exactly, moves `row_version` and records exactly one `INSTRUMENT_METADATA_UPDATED` event with the correct lifecycle facts and no description text; `testAPatchWithNoMaterialDifferenceIsAnAuditFreeNoOp`; and no file under `src/http` or `src/controllers` reaches the operation. `SharedInstrumentBoundaryTest` (7 cases) proves a V2 import declaring `instrument.active = false` cannot remove published V1 from the runtime, that a V2 rename is version-scoped rather than applied to the shared row, that V1's snapshot, checksum, row version and whole render model are unchanged by a V2 import, that a conflicting document is refused atomically with one durable `SHARED_METADATA_CONFLICT` audit and no partial write, that the authorized operation works and audits itself, that it refuses a caller that is not a principal -- an unknown GUID, an empty string, and even the id of the user who holds `instrument.manage` -- without writing (`testTheAuthorizedOperationRefusesACallerThatIsNotAPrincipal`, which replaced `testTheAuthorizedOperationRefusesAnUnknownActor` when the actor-id contract was removed), and that no route or controller reaches it. The lost-update and stale-conflict properties are proved under contention in ADM-04's metadata pairings. **Durable refusals from every refusing branch.** `testRefusedImportRollsBackAndLeavesExactlyOneDurableAudit`, `testRefusedDiscardRollsBackAndLeavesExactlyOneDurableAudit`, `testRefusedImportOfADraftInUseRollsBackAndLeavesExactlyOneDurableAudit` and `testRefusedDiscardOfADraftInUseRollsBackAndLeavesExactlyOneDurableAudit` drive the complete application-service transactions and assert the write rolled back, no row version moved, exactly one refusal audit survived with the correct actor, version, prior status, operation and reason code, no success audit was written, and a second attempt records a second attempt rather than duplicating one; `testRefusalAuditCarriesLifecycleFactsAndNoContent` asserts no definitions, snapshot or narrative content enters the record. History and red evidence: CORR6-01, CORR6-02, CORR6-03, CORR7-01. |
| Published dimensions and values are immutable (ADM-05, cross-version) | PASS at the service level. Migration `006` makes `icf.dimension_definition` and `icf.dimension_value` reporting identity only and moves every authored property to `icf.instrument_dimension` and the new `icf.instrument_dimension_value`. Executed: `DimensionVersionIsolationTest` publishes V1, imports V2 of the same instrument with the dimension renamed and a value relabelled, reordered, deactivated, dropped and another added, and proves V1's stored snapshot, checksum, row version, definitions checksum and every dimension and value property are unchanged, that V2 really did change all of them, that the shared value codes still resolve to the same `value_id` in both versions (cross-version reporting identity), that the walk path's definition index for V1 still accepts the value V2 deactivated and dropped and still rejects the one only V2 offers, and that V1 still passes every check publication made about it. `testWritingTheGlobalIdentityRowsDoesNotChangeAPublishedVersion` makes the exact writes the old importer made, directly, and proves V1 does not move. **Second correction:** migration `006`'s legacy membership backfill is now a one-time schema transition recorded in `icf.schema_migration_state`, not a condition re-evaluated on each apply. Re-evaluating it meant that once V2 minted a new global value, a re-applied `006` inferred it into published V1 -- changing V1's definitions and accepted walk values while its snapshot, checksum and `row_version` stayed put, invisible to the checksum-keyed `definitionIndex` cache. Executed: `Migration006LifecycleTest` runs the full ordered scenario against the live application database (publish V1, prime both caches, import V2 with a new value under a shared dimension, re-apply `006` through the normal mechanism) and proves V1's version-scoped rows, normalized definitions, snapshot bytes, checksum, row version, allowed walk values and render model are unchanged, from the already-primed cache **and** from a fresh uncached load, while V2 keeps its own value. `migration-006-transition.test.mjs` covers adoption of a database the earlier form already migrated (recorded `ADOPTED_PRE_STATE`, nothing inserted), a further re-application, the ambiguous half-finished transition (error 50054, whole transaction rolled back, nothing recorded), and a dimension with no global values not being read as ambiguous. `db-scripts.test.mjs` additionally proves that after the transition, a value minted globally is never inferred into the published version. `database/README.md` carries the read-only detection queries and the reviewed remediation procedure for environments the earlier form already touched. |
| ADM-02 | PASS | **Phase 6.** `GET /api/admin/instrument/versions/{id}/preview` returns `SnapshotService.renderModelFor` -- the same builder, verified snapshot and cache walks use. `InstrumentAdministrationTest.testPreviewIsTheRenderModelWalksRender`; `admin-instrument.test.mjs`: for the version `/api/instrument/current` serves, the preview model and policies are byte-identical (canonical JSON). `browser-admin.test.mjs`: the preview renders through the walk editor's `renderer.js`, every one of the 144 items and the 17 flagged placeholders appear, choosing PreK shows the PreK/K section and Grade 9 hides it, Music shows the content-area section, and no write request is made (waited past the autosave debounce). |
| ADM-06 | PASS | **Phase 6.** Clone (`/clone`), wording edit (`/edits`) and compare (`/compare`). `InstrumentAdministrationTest.testCloneEditAndCompareShowTheExactPromptChange`: a DRAFT cloned from a PUBLISHED version has the source's exact `definitionsChecksum`; one prompt is edited; the source's snapshot, checksum and `row_version` are unchanged; the comparison reports exactly one change, that item's `prompt` from its exact old text to its exact new text. Refusals: a taken label (409, audited), stale edits (409 `DRAFT_CHANGED`, audited, nothing moves), edits to a PUBLISHED version, an edit that makes the instrument invalid (422 by the shared rules), malformed requests (400 with paths), and an edit set that changes nothing (no write, no audit). `DraftEditorTest` (11) and `InstrumentVersionComparerTest` (6) prove the pure parts. `admin-instrument.test.mjs` repeats it over HTTP with the actor audited. `browser-admin.test.mjs`: New draft from the published row opens the wording editor, the prompt is changed to text carrying markup, Compare defaults to the in-service baseline and shows the before and after as text (no element created, no handler run), and a stale save is refused with a Reload action. |
| ADM-07 | PASS | **Phase 6.** `POST /versions/{id}/retire`. `InstrumentAdministrationTest.testRetiringAVersionInUseStopsNewWalksButNotHistoricalOnes`: the retired version leaves the current-version predicate, a new walk is pinned to the successor, the historical walk still renders from its own version, and snapshot, checksum, publisher and publication time are exactly as frozen; `testOnlyAPublishedVersionCanBeRetired`; `testRetiringTheOnlyInServiceVersionRequiresConfirmation`; `testReportsStillOpenWhenNoVersionIsInService`. `RetireConcurrencyBarrierTest` (3, two-sided barrier): a retirement inside a walk creation's window refuses the walk (409 `INSTRUMENT_VERSION_CHANGED`) and a retirement after the insert queues behind it -- red before the status-qualified insert (a walk pinned to a RETIRED version; a deadlock victim); and two retirements of the two in-service versions at once leave one in service, the second refused `RETIRE_LEAVES_NO_CURRENT_VERSION` -- red before the per-instrument retirement lock (both retired). `admin-instrument.test.mjs`: a historical walk on the retired version opens, loads its own instrument and saves over HTTP; the list shows the successor as in service; repeat retirement 409 and audited. `browser-admin.test.mjs`: Retire asks first, then asks again when the server says nothing would be left in service. |
| ADM-08 | PASS | **Phase 6.** `GET /versions/{id}/placeholders?q=` and the Placeholders panel. `InstrumentAdministrationTest.testThePlaceholderQueueListsExactlySeventeenWithSourceAndStatus`: exactly the 17 items, with source location and review status, searchable, shrinking to 16 when one is resolved by a DRAFT edit. `admin-instrument.test.mjs`: the same 17 keys, prompts and source locations as the supplied document, search narrows while `total` stays 17, a 201-character query is 400. `browser-admin.test.mjs`: the queue shows 17, a search by source location finds the row, its Edit link opens the wording editor on that item, and resolving it leaves 16. |
| Excel round-trip for the yearly instrument update (no acceptance ID; requested by the product owner) | PASS (automated) | `InstrumentDocumentExporterTest` (6): the supplied instrument exports and normalizes back to the same definitions checksum and whole-snapshot checksum, passes the authoring validator exactly as the supplied document does, keeps every authoring id and reference, lists rows in reading order, and a PUBLISHED version exported and imported under a new label is a DRAFT with exactly its definitions. `workbook.test.mjs` (12): the reviewed aligned workbook in `config/` (written by other software) reads to exactly the supplied instrument; download then upload with no change is exact and reproducible; a workbook re-saved by LibreOffice Calc 24.2 reads back unchanged and one edited in LibreOffice (a resolved placeholder, typed numbers and TRUE, a number in a text column, a new answer row) comes back with exactly those edits; text survives exactly; every workbook problem is named by sheet, row and column; non-workbooks, DOCTYPEs, zip bombs, encrypted and unsupported files are refused; every server path, including key-sorted normalized paths, locates its cell; the placeholder summary follows the items. `admin-instrument.test.mjs`: the document route's 401/403 posture, an edited workbook imports as a draft differing by exactly the edit, and the LibreOffice-edited workbook imports with the comparison showing exactly its five edits. `browser-admin.test.mjs`: Download gives a workbook and a JSON document equal to the version, an edited workbook uploads as a new draft, a bad reference is reported as `item_definition, row N, column response_set_id`, a workbook problem is caught before anything is sent, replacing a draft that changed since the download asks first and Cancel sends nothing, and the download panel passes axe-core. Not opened in Microsoft Excel in this environment; LibreOffice Calc is the stand-in. |
| Administration UI (A11Y-01/03/05 for the admin view) | PASS (automated) | `browser-admin.test.mjs`: every control in the view has an accessible name; the version list, the placeholder queue (reached and searched by keyboard alone), and the wording editor with an import summary each have zero serious or critical axe-core (WCAG 2.1 A/AA) violations; no horizontal page overflow at 768 and 375 px (wide tables scroll inside a focusable region). Screenshots `docs/evidence/screenshots/admin-*.png`. Real screen-reader testing was not performed. |
| RPT-01 | PASS | `ReportServiceTest.testRpt01SchoolRoleReportsOnlyItsAssignedSchool`: a school role's report is exactly its school's walks (live, as `SCHOOL_WALK_REPORT`); naming another school, another district's school or its own district is 404 through `requirePermission` on both the JSON and CSV paths for the `SCHOOL_REPORT_ONLY` role, and each refusal is audited `ACCESS_DENIED` (6 of 6 counted); inside its scope the report-only role is refused live figures (`REPORT_RELEASE_REQUIRED`, RPT-03); the options route offers only the assigned school. Over HTTP: `reports.test.mjs` "RPT-01" -- the report-only role reads its school's release (3 walks, that school only), and another school is 404 `NOT_FOUND` with no unit name in the body on the live and the released route. In the browser: `browser-reports.test.mjs` (a school walker's School filter lists only its school). |
| RPT-02 | PASS | `ReportServiceTest.testRpt02DistrictRoleAggregatesDescendantSchoolsOnly`: a district role with `include_descendants` counts both descendant schools and never the unrelated district's school (its rating of 1 is absent from the distribution); a descendant narrows, the district itself is its whole subtree, other branches are 404 for the walk-and-report and the report-only role alike; `include_descendants = 0` refuses its schools; an expired assignment is 403. The report-only district role reads releases only (`ReportReleaseTest.testOnlyCompletedWalksOfThePeriodAreReleasedWithNoStatusSplit`: its district release counts its two released schools and nothing else). `reports.test.mjs` "RPT-02" over HTTP, live and released. |
| RPT-03 | PASS (corrected; audit findings P7-01, P7C-02 and P7C-04) | Owner-approved rule (`docs/OPEN_DECISIONS.md`): k = 3, report-only users read frozen releases. **No live figures for anyone who cannot open the walks**: `ReportReleaseTest.testReportOnlyUsersCannotReadLiveFigures`, `ReportServiceTest.testRpt03...` / `testRpt01...` / `testRpt02...`, `reports.test.mjs` "RPT-03: a report-only role is refused every live figure" (400 `REPORT_RELEASE_REQUIRED` for the whole scope, one walk by answer, grade or date, drafts, JSON and CSV). **No narrowing of a release**: `testNoFilterThatChangesWhoIsCountedIsAcceptedOnARelease` (`REPORT_FILTER_NOT_PERMITTED`). **Populations**: `testOneWalkAndBelowMinimumPopulationsAreWithheldAndNeverStored` (1 and 2 walks: nothing stored, nothing categorical returned, identical to 0 walks, JSON and CSV). **Cells**: `testSmallItemCellsAndTheirComplementsAreWithheld` (option, item-state and score cells), `testASectionMeanOnTooFewRatingsIsWithheld`, `testSmallDimensionValueAndStateCellsAreWithheld` (value, state, and the Grade/Period/PreK-K group), `testOnlyCompletedWalksOfThePeriodAreReleasedWithNoStatusSplit`. **Complementary disclosure**: `testNoBreakdownWithholdsASingleRecoverableCell` and `ReportDisclosureTest` (exhaustive: every breakdown of up to six categories, k = 3 and 4, no withheld cell has fewer than two consistent values for a reader who knows the algorithm; the audit checked against brute-force inversion). **Differencing**: `testTheDistrictIsTheSumOfWhatEachSchoolPublishes`, `testReleasedDatesNeverOverlap` (service, trigger), `testAReleaseDoesNotChangeWhenWalksDo`, `ReportReleaseMembershipTest` (a walk is counted by one release at most, even after its date is corrected into later dates; a walk corrected into released dates is never released; the database refuses a second membership), `db-scripts.test.mjs` (membership, seal and immutability guards, by error number). **Never deleted** (P7C-04): `ReportReleaseTest.testNoPartOfAReleaseCanBeDeleted` (one cell, one block, one recorded walk, the release row, a block with its cells and walks, and the whole release in key order: each refused, the release reads identically) and `db-scripts.test.mjs` (the same refusals by error number 50068, and a principal with data permissions only can neither delete a release row nor disable, drop or truncate past the guard). **Isolation between concurrent requests**: `ReportIsolationTest` (two live reports on disjoint schools, and a live report and a release in both orders, run side by side behind a barrier with exact populations; no table left behind on success or failure; populations refused outside a transaction or on another connection). **Surfaces**: `testTheExportCarriesExactlyWhatTheJsonPublishes`, `testLogsAndAuditCarryNoWithheldFigureOrNarrative`, `browser-reports.test.mjs` "RPT-03 (browser)" (the page is never sent a withheld value; no walk route or live report is requested). Still: no payload, export or key names a walk, owner, observation, text, note, teacher or classroom field, and the report-only role gets 403 from `open` and `summary`. Red: `docs/evidence/phase7-correction-red-before-fix.md` and `docs/evidence/phase7-correction-2-red-before-fix.md`. |
| RPT-04 | PASS | `ReportServiceTest.testRpt04OnlyAnsweredScoresEnterTheAverage`: answered 1 and 5, one unanswered, one hidden (retaining a stored 5) and one not applicable (submitted with a 4 before the engine cleared it) give ANSWERED 2 / UNANSWERED 1 / HIDDEN 1 / NOT_APPLICABLE 1, a distribution of one 1 and one 5, and mean 3 over denominator 2. `testRetainedAnswersOfAHiddenSectionAreCountedAsHidden` does the same for a conditional section's retained answer. Red with the `ANSWERED` condition removed (`docs/evidence/phase7-red-before-fix.md`, R3). |
| RPT-05 | PASS | `ReportServiceTest.testRpt05SectionMeansArePooledFromItemLevelResponses`: component 1 answered (1, 1) in one walk and (5) in another has a pooled mean of 2.3333 over 3 responses -- not 3, the average of walk averages; across the instrument the root pools 11 scored responses (35 / 11 = 3.1818) with Part 1's ratings weighted by count. |
| RPT-06 | PASS | `ReportServiceTest.testRpt06NarrativeEmailAndTeacherFieldsNeverLeave`: sentinels in notes, both summary fields, the email draft body, subject and recipient, Observer, Lesson Standard, tags, the "Other" text, and the database-only `teacher_identifier`, `teacher_display_name`, `teacher_email` and `classroom_label` appear in none of the payload, the CSV, the `REPORT_EXPORTED` audit record or the log lines (captured through the real Logger's `buildEntry`), while the walk is still counted -- "Other" by its code. Structurally: `reports.test.mjs` fails if `ReportRepository` names any text, teacher, classroom, owner or user column. `reports.test.mjs` "RPT-03 / RPT-06" over HTTP. |
| RPT-07 | PASS | `ReportServiceTest.testRpt07EveryFilterNarrowsThePopulationWithinScope`: date from/to (inclusive, including a one-day window), school (org unit), grade, content, period, class type, visit timing, version, response option, drafts and combinations each yield the exact expected population inside the school, never another school's twin walk; section and item restrict the reported items without changing the population; a Period value retained while hidden matches neither the Period filter nor the Period distribution (HIDDEN is its own count). Red with visibility ignored (R2). `testUnknownMalformedAndOutOfContractFiltersAreRefused`: 30 refused filters, each naming its parameter. `browser-reports.test.mjs` "RPT-07 (browser)". |

## Security, privacy, operations

| ID | Status | Method / evidence |
| --- | --- | --- |
| SEC-01 | PASS (walk endpoints) | All SQL parameterized (`core/Db`; `schema-contract.test.mjs` now also covers `src/walks`). Injection payloads (`'; DROP TABLE icf.walk; --`, `1 OR 1=1`) in notes, Other text, org-unit ids, value codes, and walk ids are stored as text or rejected with 400 (`WalkServiceTest`, `walks.test.mjs`). Remaining surfaces (admin, reports) in their phases. Phase 7: every report statement is parameterized (`ReportRepository`, added to the `schema-contract.test.mjs` table scan; its only concatenations are generated placeholders and its own temp-table names, which `reports.test.mjs` checks); SQL payloads in every report parameter are refused 400 as data with no report attached (`reports.test.mjs` "SEC-01", `ReportServiceTest` validation cases), and the data is intact afterwards. |
| SEC-02 | PASS (editor, list, export, composer) | DOM construction only, `core/HtmlEncoder`, strict CSP (unchanged, and Phase 5 added no inline script or style); stored payload round trip proven by SAVE-08. Export: the text is served `text/plain; charset=utf-8` with `nosniff` and stored markup appears verbatim, neither escaped nor stripped (`walks.test.mjs` "SEC-02", `browser-email.test.mjs` "SEC-02" on the downloaded file, plus formatter cases in both suites). Composer: every stored value reaches the DOM through `textContent`/`.value`, never `innerHTML`, so `<script>` in a draft is inert text and no element is injected and no page error occurs. Admin and reports: later phases. Phase 7: `reports.js` builds the Reports view with `textContent` and DOM attributes only (`reports.test.mjs` fails on `innerHTML`, `outerHTML`, `insertAdjacentHTML`, `document.write`, `eval`); an org unit named `<img src=x onerror=...>` is printed literally in the table and the filter, creates no element and runs nothing (`browser-reports.test.mjs` "SEC-02"); the CSV neutralizes formula-leading cells (`=`, `+`, `-`, `@`, tab, CR) with an apostrophe and quotes RFC 4180 style (`ReportServiceTest.testCsvExportIsRfc4180AndNeutralizesFormulas`). |
| SEC-03 | PASS | `auth.test.mjs`: `POST /api/auth/sign-out` without or with a wrong `X-ICFWalk-CSRF-Token` is 403 `CSRF_TOKEN_INVALID` with no mutation; with the session token it succeeds. Maintenance routes are token-header authenticated (no cookies). |
| SEC-04 | PASS (local) | Session cookie observed `HttpOnly; SameSite=Lax` (`auth.test.mjs`); `Secure` enforced by configuration (`ICFWALK_COOKIE_SECURE` cannot be false in production, `ConfigLoaderTest`/`IdentityTest`); session id rotated at sign-in, invalidated at sign-out, idle timeout `ICFWALK_SESSION_TIMEOUT_MINUTES`. TLS termination is a deployment responsibility (documented). NOT TESTABLE HERE: Adobe ColdFusion's `this.sessionCookie` handling. |
| SEC-05 | PASS (saves, conflicts, rejections, exports) | `WalkServiceTest.testAuditAndMutationLogsContainNoNarrativeContent`: after saves with notes, summary text, and an observer name, no audit `details_json` or `walk_mutation.result_json` row for the walk contains them; save/conflict/rejection log lines carry walk id, codes, paths, and row versions only. Exports: `WALK_SUMMARY_EXPORTED` carries exactly `{ status, versionId, bytes }` and the `walk.summary.exported` log line the same facts, asserted by `WalkServiceTest.testSummaryAuditCarriesNoNarrative` and `walks.test.mjs` "SEC-05" (which also proves the sentence is in the exported text, so the check is not vacuous, and that no audit row anywhere carries it). Phase 7: `REPORT_EXPORTED` carries exactly `{ attempts, bytes, filters, mode, orgUnitId, rows, versionId, walks, withheld }` (`testCsvExportIsRfc4180AndNeutralizesFormulas`; plus `releaseId` for a release, `ReportReleaseTest.testLogsAndAuditCarryNoWithheldFigureOrNarrative`), `REPORT_RELEASED` exactly `{ attempts, blocks, minimumWalks, observedFrom, observedTo, releaseId, versions }`, and the `report.generated` / `report.exported` log lines carry counts only (a withheld population is `-1`); none contains any narrative, email, free-text or teacher sentinel or a walk id (`testRpt06...`). |
| SEC-06 | PASS (idempotency) / NOT TESTABLE HERE (restart) | The retry path is proven: a save whose response was lost is retried with the same `clientMutationId` and replays the committed result without duplicate rows (SAVE-03 browser, SAVE-06 service/HTTP). Restarting the application server mid-request was not automated in this environment; the mutation log is written in the same transaction as the change, so a restart leaves either both or neither. |
| SEC-07 | PASS (local) | `docs/LOCAL_SETUP.md` clean-install steps executed end to end in this environment (Docker SQL Server, schema apply, Lucee start, seed, tests). NOT TESTABLE HERE on Adobe ColdFusion 2023 itself. |

## Accessibility and responsive behavior

| ID | Status | Method / evidence |
| --- | --- | --- |
| A11Y-01 | PASS (editor, save, complete, composer) | `browser.test.mjs` "A11Y-01" plus the Phase 4 states: Save/Retry/Complete are buttons, completion-error links move focus to the control and expand its section, the conflict panel receives focus on its primary action. Phase 5: `browser-email.test.mjs` "A11Y-01" -- a part checkbox takes focus and Space toggles it, Enter on `Draft email` drafts, and every one of the composer's controls has an accessible name. Phase 7: the Reports filters are labelled controls in DOM order; End on the School select and Enter on Run report produce the report, focus stays on Run (it is never disabled under focus), and Shift+Tab returns through the form (`browser-reports.test.mjs` "A11Y-01"). |
| A11Y-02 | PASS (Phase 3 + 4 + 5 views) | Save status `role="status"`/`aria-live`; completion errors in a `role="alert"` summary, each field `aria-invalid` with `aria-describedby`, announced count; conflict panel `role="alertdialog"` with label and description; void reason field labelled. Phase 5: the part checkboxes are a `role="group"` labelled by the item's own prompt, To/Subject/Message each have a `<label for>`, copy success and copy failure are announced through a `role="status"` message (not `alert()`), and drafting and clearing are announced. Screen-reader verification with NVDA/VoiceOver remains a manual check. Phase 7: report progress and results are announced through a `role="status"` region, a refused filter through `role="alert"`, tables have captions and `th scope`, and the results region is `aria-busy` while a report runs. |
| A11Y-03 | PASS (Phase 3 + 4 + 5 views) | axe-core 4 (WCAG 2.0/2.1 A+AA) on the editor, the list, the completion-error state, and the conflict panel: no serious/critical violations. Phase 5: `browser-email.test.mjs` "A11Y-03" runs axe on the undrafted composer, the drafted composer, and the composer at 375 px -- no serious/critical violations, and no page errors in any state. Screenshots `email-composer-desktop.png` and `email-composer-phone.png`. Admin and reports: later phases. Phase 7: axe-core on the Reports view with results at desktop and at 375 px: no serious/critical violations (`browser-reports.test.mjs`); screenshots `reports-desktop.png`, `reports-phone.png`. |
| A11Y-04 | PASS (Phase 3 views) | Contrast: muted text and the REQUIRED badge darkened from the prototype values (see BUILD_STATUS); selected pills carry a check mark in addition to color; required fields carry a badge, not color alone. |
| A11Y-05 | PASS (automated part) | 375/768/1280 px: no horizontal overflow (`scrollWidth` check) with Part 1, Part 2, and a component expanded; screenshots in `docs/evidence/screenshots/`. 200 % zoom and manual review: listed under browser checks in BUILD_STATUS. Phase 7: the Reports view has no horizontal overflow at 768 and 375 px; its tables are two columns of wrapping text, so none needs a scrolling region. |

## Phase 0-4 correction session (audit findings)

Regression coverage added for the independent architecture/security/data-integrity audit.
Correction IDs are local to this session; each maps to the acceptance IDs it strengthens.
CFML evidence: `tests/cfml/specs/WalkCorrectionTest.cfc` (17 cases),
`tests/cfml/specs/WalkSchoolScopeTest.cfc` (4), `tests/cfml/specs/InstrumentScopeTest.cfc` (4).
HTTP evidence: `tests/node/walks.test.mjs` (3 added cases). Browser evidence:
`tests/node/browser-persistence.test.mjs` (3 added cases). Migration evidence:
`tests/node/db-scripts.test.mjs`, `tests/node/schema-contract.test.mjs`.

| ID | Finding | Status | Method / evidence |
| --- | --- | --- | --- |
| CORR-01 | A create replay could return a walk the current principal can no longer access (`loadDto`'s `skipAuthorization` flag was never read, and create replay ran no record-level check). | CONFIRMED, FIXED | `WalkCorrectionTest.testCorr01CreateReplayIsReauthorizedAfterAccessIsRevoked`: create at School A, revoke A, keep School B, retry the A mutation -- refused, the walk stays invisible, and the exact retry replays once access is restored. `testCorr02ReplayOfARecordedMutationOutsideScopeIsNotFound` forges a mutation row pointing at an out-of-scope walk and gets 404. HTTP: `walks.test.mjs` "a create mutation id from a school the caller lost". Strengthens WALK-03, AUTH-04, AUTH-09, SEC-06. |
| CORR-02 | Misleading `skipAuthorization` parameter. | CONFIRMED, REMOVED | `loadDto(walkId, principal)` no longer takes it; every caller authorizes the record first (`WalkService`). |
| CORR-03 | Mutation ids were bound only to actor/action/walk, so the same id could replay a different semantic request. | CONFIRMED, FIXED | Migration `004_mutation_fingerprint.sql` + `WalkCorrectionTest.testCorr03SameMutationIdWithAnAlteredRequestIsRefused` (create, save, complete, void); HTTP: "a committed mutation whose answer was lost replays on retry". Strengthens SAVE-03, SAVE-06, SEC-06. |
| CORR-04 | A create retry was refused with `INSTRUMENT_VERSION_CHANGED` once a newer version was published, stranding the committed walk. | CONFIRMED, FIXED | `WalkCorrectionTest.testCorr04CreateReplaySurvivesANewerPublishedVersion`. |
| CORR-05 | Whole-state saves relied on the browser resubmitting hidden retained values; an omitted hidden value was deleted. | CONFIRMED, FIXED | `testCorr05OmittedHiddenValueIsRetained` (Period and a conditional classroom section, asserted on `icf.walk_dimension_value` / `icf.walk_response`). Strengthens COND-06, COND-10. |
| CORR-06 | A crafted client could inject or change a currently hidden value. | CONFIRMED, FIXED | `testCorr06HiddenValueInjectionIsIgnored`: crafted hidden values are ignored and the stored values stand. Strengthens SEC-01, SAVE-07. |
| CORR-07 | Hide-then-show lost the retained value when the browser did not echo it. | CONFIRMED, FIXED | `testCorr07HideThenShowRestoresTheStoredValue`. Strengthens COND-06. |
| CORR-08 | CLEAR had to keep working under server-side retention. | VERIFIED | `testCorr08OmittingAVisibleValueClearsIt`. |
| CORR-09 | N/A clearing and note preservation had to keep working under server-side retention. | VERIFIED | `testCorr09NotApplicableClearsRatingsAndKeepsNotes`. Strengthens COND-12, COND-13. |
| CORR-10 | `clientMutationId` was optional on create and void. | CONFIRMED, FIXED | `testCorr10EveryMutationRequiresAClientMutationId`; HTTP envelope table in `walks.test.mjs`. |
| CORR-11 | `rowVersion` was optional on void. | CONFIRMED, FIXED | `testCorr11SaveCompleteAndVoidRequireARowVersionButCreateDoesNot`. |
| CORR-12 | A stale void had to refuse without changing state. | VERIFIED | `testCorr12AStaleVoidIsARefusalThatChangesNothing` (status and row version unchanged). Strengthens SAVE-04. |
| CORR-13 | An identical save to a COMPLETED walk appended a revision and advanced the row version. | CONFIRMED, FIXED | `testCorr13IdenticalCompletedSaveIsANoOpAndAMaterialOneAppendsOneRevision`; HTTP equivalent asserts `revisionCount` and `rowVersion`. Strengthens WALK-10. |
| CORR-14 | `observed_at` kept a stale date after the Visit Date was cleared. | CONFIRMED, FIXED | `testCorr14VisitDateSetThenClearedFallsBackToTheCreationInstant` (asserted against `icf.walk.observed_at` and `created_at`). |
| CORR-15 | A whole-state save accepted a missing `dimensions` or `responses` root object. | CONFIRMED, FIXED | `testCorr15WholeStateSaveRequiresBothRootContainers`; HTTP envelope table. |
| CORR-16 | CFML coercion let a JSON number or boolean stand in for a string code. | CONFIRMED, FIXED | `testCorr16JsonPrimitiveTypesAreCheckedNotCoerced` (JSON `4` is not the option code `"4"`). Strengthens SAVE-07, SEC-01. |
| CORR-17 | Browser-provided response `state` is derived data. | VERIFIED, MADE EXPLICIT | `testCorr17ClientAssertedResponseStateCannotControlPersistedState` (400 `CLIENT_STATE_NOT_ACCEPTED`, nothing written, the server's own state stands). |
| CORR-18 | `walk.org_unit_id` and the School dimension could contradict each other. | CONFIRMED, FIXED | `WalkSchoolScopeTest`: server fill and lock, School A vs School B and "Other" refused with 409 `SCHOOL_ORG_MISMATCH` and nothing written, district-authorized selection of an allowed child SCHOOL accepted, unaligned deployments still refused another school's label. Strengthens AUTH-04, RPT scope work in Phase 7. |
| CORR-19 | Current-version selection was not scoped to ICFWALK and ignored the effective window. | CONFIRMED, FIXED | `InstrumentScopeTest.testASecondInstrumentNeverSuppliesTheCurrentVersion`, `testFutureAndExpiredIcfwalkVersionsAreNotSelected`. Strengthens WALK-01, WALK-11. |
| CORR-20 | Draft discard resolved a version by label alone, across instruments. | CONFIRMED, FIXED | `InstrumentScopeTest.testDiscardDraftCannotReachAnotherInstrumentsSameLabelDraft`. Strengthens DB-06. |
| CORR-21 | The runtime snapshot was parsed and cached without checking its stored checksum. | CONFIRMED, FIXED | `InstrumentScopeTest.testACorruptedSnapshotFailsClosedOnAnUncachedLoad` (mismatch and missing digest both fail closed; the intact copy still loads). Strengthens DB-05, SEC-01. |
| CORR-22 | Failed, conflicted, or in-flight saves could be abandoned by Back/List or reload. | CONFIRMED, FIXED | `browser-persistence.test.mjs`: "a failed save plus Back/List keeps the editor and the unsaved input until an explicit decision", "an unresolved conflict plus Back/List demands an explicit discard", "beforeunload protects in-flight and queued saves". Strengthens SAVE-03, SAVE-04, SAVE-05, A11Y-02. |
| CORR-23 | Ambiguous failures abandoned the pending operation id. | CONFIRMED, FIXED | Transport failures and HTTP 5xx now retain the `clientMutationId` and the exact payload for create, save, complete, and void (`app/assets/js/app.js`); `browser-persistence.test.mjs` SAVE-03 asserts the reused id on both the transport-failure and the 5xx path. |

## Second correction session (independent verification audit)

Findings raised by the second verification audit against commit `b862af9`. All four were confirmed
against the code before anything was changed; none was rejected.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR2-01 | A replay returned the walk's *current* row version, so an old successful SAVE retried after a later successful save paired the retrying session's stale local state with a live concurrency token, and its next save overwrote the newer work with no conflict. | CONFIRMED, FIXED | `WalkReplayCoherenceTest.testAnOldSaveReplayedAfterALaterSaveCannotOverwriteIt` (the exact session A / session B / retry scenario, asserting the stored value, the stored row version, the mutation-row count, and that the returned recorded token is then refused as stale); `testAReplayIsStillIdempotentWhileTheAggregateHasNotMoved` (the recovery path still works); `WalkServiceTest.testSave06RetryWithSameMutationIdCommitsOneLogicalChange`; `walks.test.mjs` "CORR2: an idempotent SAVE replay never hands stale session state a newer row version" (two HTTP sessions, database-level assertions); `browser-persistence.test.mjs` "CORR2: a SAVE committed but lost, retried after another session saved, becomes a conflict rather than a silent overwrite" (two browsers). |
| CORR2-02 | School-dimension identity rested on `org_unit_code` = the dimension's `valueCode`. Two SCHOOL org units whose codes match no instrument School value were unprotected: any School value, including "Other", was accepted at either. | CONFIRMED, FIXED | `WalkSchoolScopeTest`: `testUnmappedSchoolUnitsAcceptNoSchoolValue` (two unmapped SCHOOL units; every value and "Other" refused 409 `SCHOOL_ORG_UNMAPPED`, nothing filled, no row-version movement, no walk created, every refusal audited), `testCodeEqualityAloneMapsNothing` (a unit whose code *is* an instrument School value still fails closed without a row), `testExplicitMappingIsFilledAndLocked`, `testAWalkAuthorizedAtSchoolACannotBeSubmittedAsSchoolB` (School B and "Other" refused 409 `SCHOOL_ORG_MISMATCH`, stored value and row version unchanged), `testOneSchoolValueBelongsToOneOrgUnit`, `testDistrictUserMaySelectAnAuthorizedDescendantSchool`; `WalkServiceTest.testCond05And06GradeFilterClearsAndHiddenPeriodIsRetained` (aligned/mapped schools continue to drive the grade filter); `walks.test.mjs` "CORR2: the School dimension follows an explicit mapping and fails closed without one" and "CORR2: the alignment endpoint derives mappings only from exact code matches"; `db-scripts.test.mjs` and `schema-contract.test.mjs` for the two uniqueness constraints that carry the invariant. Supersedes CORR-18. |
| CORR2-03 | The completion mutation id was one scalar, not scoped per walk; a void retry rebuilt its reason and row version from the live UI; and an ambiguous create, completion, or void could be abandoned by navigation or reload. | CONFIRMED, FIXED | `browser-persistence.test.mjs`: "CORR2: a CREATE committed but lost is retried with the same id and body, and yields exactly one walk", "CORR2: a COMPLETE committed but answered 5xx is retried with the same id, and never reused across walks", "CORR2: a VOID committed but lost is retried from its record, not from the edited input" (each asserting server commit then answer loss, exact id and body reuse, exactly one database effect, and the navigation/unload guard); `walks.test.mjs` "CORR2: an ambiguous outcome retries the exact same mutation id and body on every route" (CREATE/SAVE/COMPLETE/VOID replay, a rebuilt body refused, a COMPLETE id refused against another walk which stays DRAFT). Supersedes CORR-22 and CORR-23. |
| CORR2-04 | Migration 004 left pre-migration mutation rows with a NULL fingerprint, and those rows replayed as ordinary successes although nothing recorded could prove the new request was the original one. | CONFIRMED, FIXED | `WalkReplayCoherenceTest.testAnExactLookingRetryAgainstALegacyRowIsARefusal`, `testAnAlteredRequestAgainstALegacyRowIsTheSameRefusal`, `testAuthorizationAnswersBeforeTheLegacyConflict` (an authorized target gets 409 `MUTATION_LEGACY_UNVERIFIABLE`; a now-unauthorized target gets 404 with no hint, and neither writes anything); `walks.test.mjs` "CORR2: a legacy mutation row with no request fingerprint is never replayed as a success". |

## Third correction session (residual defects)

Three residual defects reported against commit `d4635b7`. Each was reproduced against the code
before anything was changed, and each regression below was run against the unfixed code first and
observed to fail. Phase 5 was not started.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR3-01 | `WalkService.replay` compared the recorded row version against an unlocked read and then built the DTO from further unlocked reads, so a concurrent save committing between the two made a replay judged coherent return the newer aggregate and a newer row version to a session still holding the original mutation's state. | CONFIRMED, FIXED | The comparison and the aggregate materialization now happen in one transaction holding the walk mutation lock (`findWalk(..., true)`), the lock every mutation path takes before it writes, and the row version is re-asserted under that lock before the DTO leaves; a mismatch at either point is 409 `MUTATION_REPLAY_SUPERSEDED`. `WalkReplayCoherenceTest.testAConcurrentSaveCannotLandBetweenTheCoherenceCheckAndTheDto` forces the interleaving deterministically through a decorating repository (`tests/cfml/support/InterceptingWalkRepository.cfc`) that fires at the DTO's first aggregate read and starts a real second session saving the same walk: that save is held by the lock (asserted, not hoped for), the replay returns the row version and the state its mutation committed, the deferred save then lands, and the token the replay returned is refused as stale. `testASupersededReplayNeverMaterializesTheNewerAggregate` proves a superseded replay refuses without reading the aggregate at all. Against the unfixed `replay` the first test fails with the concurrent save observed `COMPLETED` inside the window. Supersedes CORR2-01. |
| CORR3-02 | The School dimension's free-text option (`other`) is a value the instrument defines, so every "is this a School value?" check accepted it and it could be stored as a school's identity -- labelling that school's walks "Other" and, because the mapping is unique on (dimension, value), taking a value that names no school away from every other school. Code-equality alignment persisted mappings with no operator decision and stored the org unit's spelling of the code rather than the instrument's. | CONFIRMED, FIXED | `OrgUnitRepository.upsertDimensionMapping` refuses a non-identifying value outright (`ORG_UNIT_DIMENSION_VALUE_NOT_IDENTIFYING`), so no route can store one, and migration `005` adds `CK_org_unit_dimension_map_identifying` so neither can a script or a hand-written statement (existing rows are removed and reported on apply). `schoolValueCode: "other"` is 400 `ORG_UNIT_SCHOOL_VALUE_NOT_IDENTIFYING`. `align-school-dimension` now reports `candidates[]` and persists only pairs an operator confirms in `confirm[]`, each re-derived and re-validated, stored as the instrument spells the code; maintenance authorization is unchanged. `WalkSchoolScopeTest.testANonIdentifyingValueIsRefusedAsAnIdentityMapping` and `testASchoolUnitCodedOtherStaysUnmappedAndFailsClosed` (a SCHOOL unit coded exactly `other` stays unmapped, nothing is filled, every submitted value is refused 409 `SCHOOL_ORG_UNMAPPED`, and no mapping, School value, or row version moves); `walks.test.mjs` "CORR3: the School dimension's free-text value is never stored as a school's identity" (import refused, never a candidate, a forced confirmation refused, no mapping row added or removed, the fail-closed walk path at that unit) and "CORR3: alignment reports candidates and persists nothing without an explicit per-pair confirmation" (candidate-only by default, mismatched and non-candidate confirmations refused, the confirmed pair stored in the instrument's spelling, idempotent, unauthenticated calls refused); `schema-contract.test.mjs` and `db-scripts.test.mjs` for the constraint and the convergence path. Supersedes CORR2-02. |
| CORR3-03 | The operation registry had one `PENDING` status covering both "minted, nothing sent" and "request on the wire", so an editor change during an in-flight save deleted the record whose request had already been sent. When that answer was then lost there was nothing to retry: the committed save was never confirmed and the queued newer state went out under a new id against a row version the server had already moved past. Separately, an ambiguous `COMPLETE` or `VOID` could outlive the only control that could retry it (a destroyed confirmation row, a hidden Complete button), leaving a permanent unload guard with nothing to click. | CONFIRMED, FIXED | The status is now `UNSENT` / `IN_FLIGHT` / `AMBIGUOUS`, and only an `UNSENT` record may be replaced by a newer payload; a reload of the walk releases an `UNSENT` save record but keeps a sent one. Unresolved operations are rendered from the registry into an unfinished-operations bar outside both views, each with a retry that re-sends the exact frozen request. `browser-persistence.test.mjs`: "CORR3: editing during an in-flight SAVE keeps that save's record; a lost response retries it first, then the queued edit under a new id" and the same for "an HTTP 5xx" (the save is held on the wire, the edit lands, the answer is lost, and the three requests are asserted: the original, its byte-for-byte retry under the same id and row version, then the queued state under a new id against the row version the retry settled on, with both edits accounted for and the guard released); "CORR3: an ambiguous COMPLETE stays resolvable after the walk is reloaded from server state" (the completion commits, the stale session conflicts, reconciling reloads the walk as COMPLETED and hides its button, and the retry is still offered and replays to exactly one revision); "CORR3: an ambiguous VOID stays resolvable after navigation destroys the row that started it" (the confirmation row is gone and the walk is not listed, yet the operation is still offered and replays to exactly one void). All four fail against the unfixed browser code. Supersedes CORR2-03. |

## Fourth correction session (residual browser recovery defects)

Two residual defects reported against commit `7f7c86a`, both in browser operation recovery. Each was
reproduced against the code before anything was changed, and each regression below was run against
the unfixed `app/assets/js/app.js` first and observed to fail (9 of 9). The server, the schema, the
replay/row-version correction, the "other" org-unit identity protection, and the migration path are
untouched. Phase 5 was not started.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR4-01 | Resolving an ambiguous `CREATE`, `COMPLETE`, or `VOID` reloaded or switched the editor unconditionally (`refreshAfterResolvedOp` → `openWalk`, and the create/superseded paths), and `openWalk` cancels the scheduled save, replaces the editor state with the server copy, and clears `dirty`. A change typed after the operation went ambiguous was therefore lost the moment the user pressed Retry — inside the 700 ms before its own autosave had been dispatched, so nothing carrying it had left the browser and the server never saw it either. Separately, a `SAVE` whose answer arrived after the editor had moved on returned early from `app.current !== walk` before settling its record, leaving the operation `IN_FLIGHT` for good: unfinished work the unload guard sees, on an operation the recovery bar never lists. | CONFIRMED, FIXED | `editorHoldsNewerWork()` is the one test recovery reads before it reloads, replaces, or leaves an editor (dirty, debounced save, save on the wire, failed save, conflict, or any pending `SAVE` record). When it holds: a resolved `COMPLETE` adopts the resolved aggregate's metadata only (`adoptResolvedWalk`) — status, the row version the replay proved it still stands at, and its stamps — and never the state on screen, so the waiting autosave goes out against the right token and carries the newer edit through; a resolved `CREATE` leaves the editor alone and says the walk is waiting in My walks; a resolved `VOID` asks before it takes the editor away (`UNSAVED_VOIDED`), and staying keeps the text on screen under a live unsaved-work guard (the server's 409 `WALK_VOIDED` is the definitive answer that asks again). `saveCurrent` now settles its record from the save's own outcome — `settleOp` on success and on a definitive 4xx, `markOpAmbiguous` on an ambiguous one — before any editor check, so a save can no longer be stranded; only the editor-facing work and the rescheduling are skipped when the editor has moved on. `browser-persistence.test.mjs`: "CORR4: retrying an ambiguous COMPLETE keeps a newer editor change that autosave has not dispatched" (edit and Retry in one browser task, so the interleaving is pinned inside the debounce; no PUT had left the browser; the edit survives, the single autosave goes out against the row version the completion committed, and no conflict is needed), "CORR4: an ambiguous COMPLETE resolved while a SAVE is on the wire strands neither operation" (the PUT is held at the browser boundary, the completion replays underneath it, the released save is refused as stale and reconciles through the conflict panel, and the unload guard is released), "CORR4: retrying an ambiguous CREATE never switches the editor away from a dirty walk", "CORR4: retrying an ambiguous CREATE while another walk's SAVE is on the wire strands neither", "CORR4: a resolved VOID never leaves a dirty editor without an explicit decision". Supersedes CORR3-03 for the recovery paths. |
| CORR4-02 | An ambiguous operation stayed `AMBIGUOUS` for the whole of its retry round trip, so the recovery bar kept offering Retry and a second activation dispatched the same mutation id again. Server idempotency prevented duplicate database effects, but the client lifecycle was wrong and the two answers raced each other through the refresh on the way back. | CONFIRMED, FIXED | `markOpSent` now transitions `AMBIGUOUS` → `IN_FLIGHT` as well as `UNSENT` → `IN_FLIGHT`, synchronously before the request is created, so there is no window between the test and the transition. `resolveAmbiguousOp` refuses an `IN_FLIGHT` record outright, and so do `startNewWalk`, `completeCurrent`, and the void confirmation (`opInFlight`), so no control can dispatch a second request for the same mutation id. The recovery bar keeps a retrying record on screen with both controls disabled (`recoveryOps`, `data-op-status`), so "already retrying" is visible rather than the bar blinking out. An ambiguous outcome for the retry itself returns the record to `AMBIGUOUS` and restores the controls; definitive failures, conflicts, and `MUTATION_REPLAY_SUPERSEDED` keep the existing lifecycle contract. `browser-persistence.test.mjs`: "CORR4: activating Retry twice on an ambiguous CREATE / COMPLETE / VOID dispatches one request and offers no second Retry" (the retry is held on the wire, Retry is activated twice in one browser task, exactly one request carries the mutation id, both controls read disabled, and a third activation forced past the disabled attribute is still refused by the lifecycle), "CORR4: a retry that is itself ambiguous returns the operation to AMBIGUOUS with live controls". |


## Fifth correction session (mutation response coherence)

The single remaining Phase 0-4 freeze blocker, reported against commit `fa59b16`. It was reproduced
against the code before anything was changed: all five regressions below were run against the
unfixed `src/walks/WalkService.cfc` first and observed to fail (5 of 5), then against the corrected
file and observed to pass (5 of 5). Raw evidence: `docs/evidence/correction5-red-before-fix.txt`.
No schema migration was required and Phase 5 was not started.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR5-01 | `WalkService.save()` and `complete()` locked the walk, applied the mutation, recorded the resulting rowversion and committed, then called `loadDto()` to build the response — after the transaction had ended and the walk mutation lock had been released. Session A could commit M1 and produce R1; session B could then save M2 against R1 and commit R2; and A's later `loadDto()` would return B's aggregate and R2. Because the browser adopts the returned metadata without replacing the editor state with `saved.state` (`app/assets/js/app.js::saveCurrent`), A was left holding stale local state paired with a live R2 token, and a later whole-state save would overwrite B without ever receiving `STALE_ROW_VERSION`. `complete()` had the identical problem, and leaked more through it: status, `completedAt` and the revision count are part of that response too. | CONFIRMED, FIXED | The response DTO is now materialized inside the mutation's own transaction, while the `findWalk(..., true)` lock is still held, and returned through the transaction outcome (`mutationDto()`; `save()` and `complete()` no longer call `loadDto()` after the commit). The rowversion, header, dimensions, responses, evaluation states and revision count therefore all describe the state that mutation produced, and the DTO rowversion is re-asserted against the rowversion the mutation recorded in `icf.walk_mutation` before it leaves — a mismatch rolls the transaction back rather than serving an incoherent response. The successful no-op SAVE on an already completed walk takes the same path. `WalkMutationResponseTest` forces the interleaving deterministically, never by sleeping or racing: `tests/cfml/support/InterceptingDb.cfc` decorates `Db.transact` and runs a one-shot callback at the exact instant the mutation transaction commits and before the service method resumes, and that callback is a real second session committing a real second mutation through the real service against the real database. `testASaveAnsweredWithItsOwnStateWhenAnotherSessionCommitsAtTheCommitBoundary` is the reported scenario end to end (A saves M1 against R0 and produces R1; B saves M2 against R1 and commits R2 at the boundary; A still returns M1/R1 and A's aggregate, asserted against the recorded mutation row; the database ends with B's M2/R2; A's returned R1 is then refused 409 `STALE_ROW_VERSION`; B's data is unchanged after that conflict). `testACompleteAnsweredWithItsOwnStateWhenAnotherSessionCommitsAtTheCommitBoundary` is the analogue for COMPLETE, additionally asserting the revision count as of that completion while B's post-completion edit adds its own. `testANoopSaveOnACompletedWalkIsAlsoCoherent` covers the no-op branch. `testTheResponseCoherenceInvariantHoldsForBothSaveAndComplete` drives the one invariant over both operations from a single table so neither can drift from it. `testNoConcurrentWriterCanCommitWhileTheResponseIsBeingBuilt` proves the stronger form through `tests/cfml/support/InterceptingWalkRepository.cfc` firing at `countRevisions` (reached only while the response is being materialized) and starting a real concurrent writer there: it is still blocked on the walk mutation lock when the bounded 6000 ms join expires, then runs to completion and earns `STALE_ROW_VERSION` once the lock is released. Against the unfixed service all five fail — four with the response carrying the rowversion the *other* session committed, and the fifth with the concurrent writer observed `COMPLETED` inside the window. The atomic replay path and `MUTATION_REPLAY_SUPERSEDED` (CORR3-01) are unchanged and still pass. Strengthens SAVE-04, SAVE-05, WALK-08. |


## Phase 6 publish-foundation third correction (superseded in part by the fourth correction below)

Seven residual defects reported against commit `bff53f53eaacfed977eefd4648577ed5cbba196c`, whose
independent verdict was **NOT READY TO ACCEPT THE PHASE 6 PUBLISH FOUNDATION**. Each focused
regression below was written first and run against the audited production code before anything was
changed; the raw red results are in `docs/evidence/phase6-third-correction-red-before-fix.md` and
the complete final gate transcript in `docs/evidence/phase6-third-correction-release-gate.txt`.
No Phase 6 administration feature and no Phase 7 work was started. **This is a correction candidate
awaiting independent verification: Phase 6 is not frozen, not complete and not accepted.**

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR6-01 | `InstrumentMetadataService.updateMetadata` read `icf.instrument` with an unlocked `findInstrumentByCode`, derived a complete replacement row from it, and only then called `updateInstrumentMetadata`, where the row lock was taken. A lock acquired after a read does not protect that read: two concurrent partial patches both restated what they had read before the other ran, so the second to commit silently undid the first, and each audit event could carry a `before` image that was never the row it replaced. Because `icf.instrument.active` is part of `SnapshotService.currentVersion()`'s predicate, the undone change could be a deliberate deactivation of a published version. Import had the same shape: it judged `SHARED_METADATA_CONFLICT` against an object read before it locked the version. | CONFIRMED, FIXED | `DefinitionRepository.lockInstrumentByCode` / `lockInstrumentById` take the row under `UPDLOCK, HOLDLOCK, ROWLOCK` and read it in the same statement; the metadata operation's locked read, merge, update and audit are one transaction, and `updateInstrumentMetadata` performs only the UPDATE. Import re-reads the row under that lock **after** its version lock, preserving the version-then-instrument order, and decides the conflict on it. `SharedMetadataConcurrencyBarrierTest`: `testADeactivationIsNotLostByAConcurrentRename` and `testARenameIsNotLostByAConcurrentDeactivation` force real contention in both arrival orders through `ConcurrencyBarrier` and assert both independent changes survive; `testEachAuditDescribesTheRowItActuallyReplaced` asserts the second writer's `previousActive` is what the first committed, not what it first read; `testImportDecidesTheSharedConflictOnTheLockedCurrentRowNotItsEarlierLookup` commits an authorized deactivation between import's read and its conflict check and requires the refusal; `testImportQueuesForTheSharedRowAndThenDecidesOnIt` proves the same under real lock contention and that the two lock orders queue rather than deadlock; `testAConflictRefusalMovesNothingAndLeavesOneDurableAudit` asserts no version row, definition row, snapshot, checksum, shared row or row version moved and exactly one durable refusal audit exists. Red, behaviourally, against the audited code: the final row kept the pre-contention name (lost update) and the racing import committed. |
| CORR6-02 | The metadata operation required a GUID actor and the repository checked `userExists`. That is an integrity check for `icf.audit_event.actor_user_id`'s foreign key -- satisfied by every row in `icf.app_user` -- and it was standing in for authorization, so any existing user could be named as the alleged authorizer by any internal caller. The existing suite's successful actor had never been granted `instrument.manage`. The absence of an HTTP route is not authorization. | CONFIRMED, FIXED | `updateMetadata` takes the current principal and calls `AuthorizationService.requirePermission(principal, "instrument.manage")` before any mutation; there is no actor argument, and the audit actor is the principal's `userId`. `Bootstrap` constructs the service after `authorizationService`. `userExists` stays and is documented in source, `ARCHITECTURE.md`, `DATA_CONTRACT.md` and `OPEN_DECISIONS.md` as an integrity check, not authorization. `InstrumentMetadataServiceTest` (15 cases at this correction; 17 after CORR7-01 added the two case-sensitivity regressions): a known, active user holding a real `DISTRICT_WALK_REPORT` assignment is denied 403 and changes nothing; so is a user with no assignment; a user with a real effective `MASTER_INSTRUMENT_ADMIN` assignment succeeds; the stored and audited actor is the principal and a supplied `actorUserId` argument is not honoured; a bare user id, an empty string and an empty struct are all `INSTRUMENT_METADATA_PRINCIPAL_REQUIRED`; blank, over-length, non-string `name`, over-length and non-string `description`, `"false"`/`"no"`/`0`/`1`/arrays/objects for `active`, unknown members and an empty patch are each refused before any write with the documented code and no audit; a patch with no material difference is an audited-free no-op that does not move `row_version`; a missing instrument is refused without auditing success; the description's text never enters the audit event; and no file under `src/http` or `src/controllers` names the service or the operation. Red against the audited code: a user with no `instrument.manage` renamed the shared row and it committed. |
| CORR6-03 | `createDimensionIdentity` and `createDimensionValueIdentity` called `requireDraftVersion` and then issued an unconditional INSERT. Both are public repository methods and are called outside any transaction, where the `UPDLOCK` lives only for the length of the SELECT -- so a publish could freeze the version between the check and the write, and permanent global reporting identity was minted on the authority of a version that was no longer a DRAFT. | CONFIRMED, FIXED | Each creator is now one `INSERT ... SELECT` whose source is the owning `icf.instrument_version` row under `UPDLOCK, ROWLOCK`, predicated on `status = N'DRAFT'`, with `OUTPUT INSERTED` returning the inserted row; nothing eligible means nothing inserted and a typed non-DRAFT refusal rather than a generated GUID. `GlobalIdentityBoundaryTest`: `testMintingIdentityIsQualifiedByItsOwnStatement` asserts the structure over the real production SQL (one statement, the version row under `UPDLOCK`, the `status = N'DRAFT'` predicate, `OUTPUT INSERTED`, and no separate `requireDraftVersion` whose lock would be released first) -- asserted structurally because the window it closes is *between two statements*, and observing it from outside would need a production seam this correction is forbidden to add; `testBothCreatorsWriteForADraftOutsideAnyTransaction`; `testAFrozenVersionCannotMintIdentityAndNothingMoves` compares a SHA-256 fingerprint of both global tables across PUBLISHED and RETIRED refusals; `testAnAbsentVersionCannotMintIdentity`; `testWhenTheMintWinsExactlyOneIdentityAppearsAndPublishStillSucceeds` and its value-identity twin force the serial outcome where the mint wins. The converse -- publish wins, the mint is refused, and the global tables are byte-identical -- is `PublishConcurrencyBarrierTest.testPublishVersusNewDimensionIdentityReachesOneSerialOutcome` and `…NewDimensionValueIdentity…` under the two-sided barrier. Red against the audited code: the structural case failed on the absent `status = N'DRAFT'` predicate in the minting DML. |
| CORR6-04 | `PublishConcurrencyBarrierTest` held transaction A, started B, joined B with `threadJoin(..., 6000)` and treated `status != "COMPLETED"` as proof that B had reached the competing row lock. That is an inference: a scheduler delay, a datasource-pool wait or setup work inside B produces the same observation, and so does an implementation whose lock does nothing on a run where B merely started late. | CONFIRMED, FIXED | `tests/cfml/support/ConcurrencyBarrier.cfc` carries two announcements across the thread boundary on `java.util.concurrent` structures, and `InterceptingDefinitionRepository` gained `armBefore` alongside `armAfter`. A emits `A_LOCKED` from inside its transaction holding the production lock and having done nothing else; B emits `B_AT_COMPETING_BOUNDARY` immediately before the database call that will contend for it; the spec blocks on a real `LinkedBlockingQueue` poll (no CFML sleep) and releases A only after hearing B, then asserts `signalledInOrder` and, only as a supplemental fact, that B had not completed. Seven deterministic cases: publish/publish, publish/import, import/publish, publish/new dimension identity, publish/new dimension-value identity (`PublishConcurrencyBarrierTest`), metadata/metadata and import's shared-state check/authorized metadata update (`SharedMetadataConcurrencyBarrierTest`). The `Promise.all` stress races in `admin-publish.test.mjs` remain as supplemental coverage. The interception components are test-only: nothing in `src/` references them, the container never holds them, and no route, environment flag or remotely activatable hook reaches them. Red against the audited test support: every case failed with `armBefore` absent -- B's arrival could not be observed at all, which is the defect. |
| CORR6-05 | `DefinitionValidator.checkCounts` used `isNumeric` and loose comparison, and in CFML the java.lang.String `"144"` is numeric for coercion and compares equal to `144`, so a snapshot whose counts block was `{"items": "144", ...}` passed the envelope and published. The existing `"many"` case proved only that non-numeric text was rejected. Separately, publication accepted `isJSON("null")`, deserialized it and handed the result to the canonical serializer; on Lucee a top-level JSON null leaves the variable null and reading it back is an engine error, which fired before `markRefusal`, so the caller received a 500 and the attempt left no durable refusal audit. | CONFIRMED, FIXED | `src/core/JsonTypes.cfc` decides JSON type from the value's Java class. Counts must be actual JSON numbers -- strings, booleans, arrays and objects are `SNAPSHOT_COUNTS_INVALID` type errors -- and whole, non-negative, finite and within the supported integer range, with equality compared between known whole numbers. `DefinitionValidatorTest`: `testEveryCountMemberRefusesAJsonStringEvenWhenItNamesTheRightNumber` is table-driven over all nine members and asserts the Java class of the value it plants; plus booleans, arrays and objects, and negative, fractional and oversized numbers, each at `$.counts.<member>`. At publication, `InstrumentPublishServiceTest.testACountStoredAsTheExactMatchingStringIsRefused` stores `"144"` with a matching checksum and canonical bytes and asserts the typed 422, the stable code and path, and an untouched DRAFT with exactly one refusal audit; `testNegativeFractionalAndOversizedCountsAreRefused` covers the range rule. For the shape guard, publication now establishes that the parsed snapshot is a JSON object before anything dereferences or serializes it: `testATopLevelNullSnapshotIsARefusalAndNotAnEngineError`, `testATopLevelArraySnapshotIsRefused` and `testATopLevelScalarSnapshotIsRefused` each store the exact bytes with their real checksum and assert 422 `INSTRUMENT_VERSION_NOT_PUBLISHABLE`, a stable `SNAPSHOT_SHAPE` issue at `$`, status still DRAFT, snapshot and checksum bytes unchanged, row versions, publisher and timestamps unchanged, exactly one durable refusal audit and no success audit. Those cases also prove the *database's* own defence first: `CK_instrument_version_snapshot_json` (a 2016-era `ISJSON`) refuses to store a top-level scalar at all, so the constraint is dropped for the length of the store, restored immediately, asserted back, and re-validated in `afterAll`. Red against the audited code: `"144"` published successfully, `2147483648` was reported only as a mismatch, and the top-level null failed with `variable [SNAPSHOT] doesn't exist` -- an engine error, not a refusal. |
| CORR6-06 | The remediation sample in `database/README.md` deleted version membership by `version_id` and `value_code`. `icf.dimension_value.value_code` is unique only within a dimension, and the supplied instrument uses `other` under `school`, `content` and `classType`, so an operator who approved one `(dimension_code, value_code)` pair removed three. `@@ROWCOUNT` reports 3 either way. | CONFIRMED, FIXED | The sample is replaced by an exact-identity procedure: a signed-off pair list whose primary key rejects a repeated pair; resolution of each pair to an exact `(version_id, dimension_id, value_id)` triple, displayed before any mutation; a typed refusal (50060) for anything missing, ambiguous or unresolvable; a transaction with rollback as the default; a delete of only the resolved rows; `OUTPUT ... INTO` capture of what was really deleted; an `EXCEPT` comparison of the deleted and approved sets in both directions (50061); and a commit only after that check and an explicit `@signedOff`. `tests/node/remediation-exact-identity.test.mjs` extracts the SQL **from the document** so the procedure an operator would run is the one proved, applies 001..006 to a disposable database, builds the repeated `other` code under three dimensions on a PUBLISHED version, and asserts: the pre-correction sample removes all three and reports `removed = 3`; an un-signed-off run of the new procedure removes nothing; a signed-off run removes only `school/other`; all three global `other` identities survive; the frozen checksum does not move; an unresolvable pair and a pair naming a dimension the version does not place are each refused 50060 with nothing removed; and a duplicated approval fails on the list's own primary key (2627). Error 50061 is asserted structurally only -- the SQL extracted from the document must contain `THROW 50061` -- and is not behaviourally exercised (CORR7-03). Red against the audited README: the document published no exact-identity block at all. |
| CORR6-07 | `InstrumentConfigValidator.checkInstrument` rejected a non-DRAFT status only when `instrument.version.status` was present, so a document that omitted it imported -- silence read as consent on the one field that says which of three lifecycle states the author believes they are writing. CFML's `!=` is case-insensitive besides, so `draft` passed as a DRAFT declaration. | CONFIRMED, FIXED | The status is required, must be a JSON string, and must equal `DRAFT` case-sensitively (`compare`), reported at `$.instrument.version.status` with three distinct stable codes: `VERSION_STATUS_REQUIRED` (absent or null), `VERSION_STATUS_INVALID` (present, not a string), `VERSION_STATUS_NOT_DRAFT` (a string that is not exactly `DRAFT`). Persisted-version lifecycle checks are not duplicated here. `InstrumentConfigValidatorTest`: `testAMissingStatusIsRejected`, `testANullStatusIsRejected`, `testANonStringStatusIsRejected` (numbers, booleans, arrays, objects), `testABlankStatusIsRejected`, `testALowerCaseStatusIsRejected` (`draft`, `Draft`, `dRaFt`), `testNonDraftStatusIsRejected` (PUBLISHED and RETIRED, with the path asserted) and `testAnExactDraftStatusIsAccepted`. Red against the audited code: a document with no status was valid, a lower-case one was valid, and a numeric one was reported as a wrong *value* rather than a wrong type. |


## Phase 6 publish-foundation fourth correction (superseded in part by the fifth correction below)

Four audit findings (0 HIGH, 2 MEDIUM, 2 LOW), plus one found by this pass's own exact-commit gate (CORR7-05), reported against commit
`a8e97f22ae1639faef5b6e68bf7255dea838f8e2`, whose independent verdict was **NOT READY TO ACCEPT THE
PHASE 6 PUBLISH FOUNDATION**. The audit found the seven CORR6 defects materially closed; those fixes
are preserved unchanged. The red result for the one production defect is in
`docs/evidence/phase6-fourth-correction-red-before-fix.md`. No Phase 6 administration feature and
no Phase 7 work was started, and no schema, route, dependency or production hook was added. **This
is a correction candidate awaiting independent verification: Phase 6 is not frozen, not complete
and not accepted.**

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR7-01 | `InstrumentMetadataService.updateMetadata` decided whether a field changed with `toString(before[field]) != toString(after[field])`. CFML's string `!=` is case-insensitive, so a legitimate capitalization-only edit (`ICFWalk` to `Icfwalk`, or the same for a description) was classified as a no-op: nothing written, `row_version` unmoved, no audit event. | CONFIRMED, FIXED | `name` and `description` are compared with `compare()`, which is case-sensitive, and `active` as a boolean (`XOR`), never through `toString`. The locked read, transaction, authorization, actor derivation, validation, narrative exclusion and absent route are unchanged, and so is the public method contract. `InstrumentMetadataServiceTest.testACaseOnlyNameChangeIsMaterial` and `testACaseOnlyDescriptionChangeIsMaterial` each assert the authorized principal is the actor in the result and in the audit, `noOp` is false, `changedFields` is exactly the one field, the exact requested capitalization is stored, `row_version` moved, exactly one `INSTRUMENT_METADATA_UPDATED` event was added with the correct `previousName`, `name`, `previousActive`, `active` and `descriptionChanged`, and no description text appears in the audit JSON. Their value assertions use a local byte-exact `compare()` helper, because `BaseSpec.assertEquals` compares with the same case-insensitive operator (CORR8-01 replaced that helper with the shared `BaseSpec.assertExactTextEquals`, and the two cases' row-version checks with `assertRowVersionChanged`). `testAPatchWithNoMaterialDifferenceIsAnAuditFreeNoOp` still passes, so the no-op contract for genuinely unchanged values holds. Red against the unmodified production code: both new cases failed with `changedFields=[]` and the old capitalization still stored. |
| CORR7-02 | The primary ADM-04 and ADM-05 rows still described the superseded implementation: ADM-04 said B's non-completion proved it had reached the competing lock and listed only three contention pairings; ADM-05 said the global identity creators call `requireDraftVersion` before an INSERT and that `updateInstrumentMetadata` requires a named known `icf.app_user`, and cited the removed `refuses an unknown or blank actor` case. | CONFIRMED, FIXED | Both rows were rewritten in place (above), not annotated. ADM-04 now describes the two independently observed signals (`A_LOCKED`, `B_AT_COMPETING_BOUNDARY`), states that non-completion is supplemental only, and names all seven pairings with their test methods, in agreement with CORR6-04. ADM-05 now describes the single status-qualified `INSERT ... SELECT` with `OUTPUT INSERTED`, the current-principal requirement, central `instrument.manage`, actor derivation from `principal.userId`, and the current test names and counts, in agreement with CORR6-01..03 and CORR7-01. `docs/ARCHITECTURE.md` and `docs/DATA_CONTRACT.md` carried the same superseded barrier description and ARCHITECTURE's lock-order paragraph named `requireDraftVersion` for every mutator; those statements were corrected, and the metadata no-op statements in `DATA_CONTRACT.md` and `OPEN_DECISIONS.md` now say that materiality is case-sensitive. `BUILD_STATUS.md`'s earlier correction sections are historical and labelled as superseded; the mutation inventory was already accurate and is unchanged. |
| CORR7-03 | The third correction's evidence ledger overstated two facts: its environment record said 26 CFML spec files ran, and its red-before-green record said the remediation regression exercised errors 50060, 50061 and 2627. It also did not record that `testTheAuthorizedOperationRefusesAnUnknownActor` had been replaced. | CONFIRMED, FIXED | `docs/evidence/phase6-third-correction-environment.md` now says 31 and records the reconciliation: `git ls-tree` at `a8e97f2` lists 31 `tests/cfml/specs/*Test.cfc` files, and the raw transcript reports exactly the same 31 distinct spec class names. `docs/evidence/phase6-third-correction-red-before-fix.md` now says 50060 and 2627 were executed and 50061 was asserted structurally but not behaviourally exercised; no artificial 50061 path was added. It also records that `testTheAuthorizedOperationRefusesAnUnknownActor` was replaced by `testTheAuthorizedOperationRefusesACallerThatIsNotAPrincipal` because the service no longer accepts an actor id, and that the replacement is stronger (it also refuses the id of a user who holds `instrument.manage`), so no behaviour coverage was removed. The raw transcript `phase6-third-correction-release-gate.txt` is unchanged as historical evidence. |
| CORR7-04 | The third correction ran its live gate at `bff53f5` with a dirty working tree and committed afterwards, so its transcript did not prove that `a8e97f2` was the exact tree tested. | CONFIRMED, FIXED | This correction is committed first, the working tree is confirmed clean including untracked files, and the complete clean-database release gate then runs from that exact commit, with the transcript captured outside the repository and the identity checks (`HEAD`, `HEAD^{tree}`, porcelain status, both diffs, baseline ancestry, remote ref) recorded before and after. The raw final transcript and the final environment record are handoff artifacts, not repository files, because adding them after the gate would create a different commit. The browser suites' screenshots are redirected outside the repository for that run through the new test-harness variable `ICFWALK_SCREENSHOT_DIR` (default unchanged), since Playwright's PNG bytes differ run to run and would otherwise change the tree being proved. The first exact-commit gate, at `c7f6f48a36626dc357bd36aa65db85a26083f15a`, failed; see CORR7-05. |
| CORR7-05 | Found by this pass's own first exact-commit gate, not by the audit. (a) The new `testACaseOnlyDescriptionChangeIsMaterial` asserted that `row_version` moved with `BaseSpec.assertNotEquals`, whose CFML `==` compares numeric-looking strings as numbers: `000000000000E988` reads as `0e988`, which is zero, and equalled its successor. (b) The pre-existing `InstrumentPublishServiceTest.testSemanticOptionInAForeignResponseSetIsRefused` moved a response set's options into an arbitrary set of a second draft chosen by an unordered `TOP (1)`; 585 of 841 possible pairings in the supplied instrument share an option key or stored code, so its setup could hit `UQ_response_option_set_code` depending on plan and ids. | CONFIRMED, FIXED (test-only) | (a) Both new cases compare row versions with `compare()`; the coercion was observed directly on Lucee 6.2.8.20 with a temporary, uncommitted probe (`"000000000000E988" == "000000000000E989"` is `true`, `compare()` is -1). (b) The receiving set is chosen, ordered, from sets sharing neither `option_key` nor `stored_code` with the moved options, and its existence is asserted as a precondition; the publication assertions are unchanged. The failed transcript is kept with the handoff artifacts, and the exact-commit gate is repeated from the commit that carries these fixes. Not changed: 87 existing call sites across 16 spec files still pass row versions to `BaseSpec`'s coercive equality, which could let an "unchanged" assertion pass although the version moved; recorded as a follow-up in `BUILD_STATUS.md` (closed by CORR8-01). |

## Phase 6 publish-foundation fifth correction (frozen as the Phase 0-6 baseline)

The independent audit of the fourth correction (`04fb1afb05edbdad96d61c160c59217a01ab06e0`) found 0
HIGH findings, one new MEDIUM (CORR8-01), one new LOW (CORR8-02), and the prior MEDIUM P6C3-01 still
open (CORR8-03); its verdict was **NOT READY TO ACCEPT THE PHASE 6 PUBLISH FOUNDATION**. It found
P6C3-02, P6C3-03 and P6C3-04 closed (CORR7-02, CORR7-01 and CORR7-03 above); those corrections are
preserved unchanged. No production source, schema, route, dependency, production hook or tag was
added or changed, and no Phase 6 administration feature or Phase 7 work was started. **This is a
correction candidate awaiting independent verification: Phase 6 is not frozen, not complete and
not accepted.**

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| CORR8-01 | `BaseSpec.assertEquals` / `assertNotEquals` compared stringified values with CFML's `!=` / `==`, which ignore case and read numeric-looking text as numbers. Exact-contract assertions -- immutability, refusal atomicity, no-ops, optimistic concurrency, replay, response coherence, concurrency outcomes -- could therefore pass although the value moved, or fail although it did: `000000000000E988` equals `000000000000E989` under `==` (both zero), and `ICFWalk` equals `Icfwalk`. 90 row-version comparisons in 16 spec files went through those helpers, besides hundreds of comparisons of codes, identifiers, checksums, canonical JSON and authored text. | CONFIRMED, FIXED (test harness) | Red before any helper existed: a temporary probe calling the unmodified helpers showed `assertEquals` accepting both pairs and `assertNotEquals` rejecting both valid inequalities (4 failed, 2 controls passed, exit 1), `docs/evidence/phase6-fifth-correction-red-before-fix.md`. `BaseSpec` gains `assertExactTextEquals`, `assertExactTextNotEquals`, `assertRowVersionEquals`, `assertRowVersionChanged` and `assertExactJsonEquals`, all on `compare()`, with no normalization of case, prefix, padding or representation, refusing null, structures (for the text helpers) and empty row versions, and naming both values and their first difference on failure. Every exact-contract call site was migrated after a traced run of the unmodified suite showed exact and coercive comparison agreeing in all 3,999 executions (so no expectation changed): 548 text, 25 text inequality, 92 row-version (78 equality, 14 change), 23 structure; the general helpers keep 275 numeric and boolean comparisons, each listed with its contract reason in `docs/evidence/phase6-fifth-correction-assertion-inventory.md`. Assertion-deciding `==` predicates on codes, paths, states and barrier signals now use `compare()`; `assertThrows` compares the errorcode exactly. `ExactAssertionTest` (14 cases) proves both directions of every helper with both pairs, and guards the result: the general helpers refuse row-version-shaped operands at run time, and a source scan fails on a row-version expression or a text literal passed to them (500 findings before the migration, none after). A traced run of the migrated suite showed the general helpers receiving only numbers and booleans outside that spec. |
| CORR8-02 | The fourth correction's section of `BUILD_STATUS.md` labelled the case-only metadata item (P6C3-03) MEDIUM and the exact-commit traceability item (P6C3-01) LOW; the audit classified them LOW and MEDIUM. | CONFIRMED, FIXED | The labels now read `1 (LOW; audit P6C3-03)`, `2 (MEDIUM; audit P6C3-02)`, `3 (LOW; audit P6C3-04)` and `4 (MEDIUM; audit P6C3-01)`. The aggregate is unchanged at 0 HIGH, 2 MEDIUM, 2 LOW, and so is the substance of every item. The CORR7 ledger above states only that aggregate, and the fourth correction's evidence records carry no per-item severity, so nothing else needed correcting. |
| CORR8-03 | P6C3-01, carried open: the fourth correction's handoff supplied a repository archive but not the raw transcript of its final exact-commit gate or its final environment record, so the gate could not be tied to the exact final pushed commit. | ADDRESSED IN THE HANDOFF, awaiting verification | This correction is committed first; the working tree and index are confirmed clean (tracked and untracked); the complete clean-database gate (npm ci, handoff validation, package tests, JS/MJS syntax, a new disposable SQL Server container, 001..006, re-application and the 001 refusal, startup, seed, restart, `ICFWALK_REQUIRE_APP=1 npm test` with screenshots outside the repository, cleanup) runs from that exact commit with its raw transcript captured outside the repository; identity is recorded before and after; the commit is pushed normally and the remote branch hash is shown equal to the tested commit; the repository ZIP is made from that pushed commit. The ZIP, the transcript and the environment record are separate handoff artifacts, each with its SHA-256, and none is committed, because committing them would create a different, untested commit. |

## Phase 7: aggregate reporting (current state)

Implemented from the frozen Phase 0-6 baseline `a219d9e0987b85b1a0b587fd62effa4e0ad1ffde`. RPT-01..07
are recorded in their own rows above, together with the Phase 7 additions to AUTH-01, AUTH-06,
SEC-01, SEC-02, SEC-05, A11Y-01, A11Y-02, A11Y-03 and A11Y-05. The deterministic coherence proof
and the four red-before-fix runs are in `docs/evidence/phase7-red-before-fix.md`. Every CFML-backed
PASS was produced on Lucee 6.2.8.20 against SQL Server 2022 and still has to be re-run on Adobe
ColdFusion 2023 and on SQL Server 2016 (see the runtime note at the top of this file). **This is a
Phase 7 candidate awaiting independent audit: Phase 7 is not frozen and not accepted.** The
correction of the audit's findings P7-01 and P7-02 is recorded in the ledger at the end of this
section.

| Area | Status | Evidence |
| --- | --- | --- |
| Report coherence (a walk is counted in one committed state) | PASS | `ReportCoherenceTest` (4 cases): a real save committed between the dimension and item aggregates is detected and the report recomputed as the new state; a walk voided mid-report leaves the population cleanly; a population that never holds still is refused with 409 `REPORT_POPULATION_CHANGED` after three attempts; a quiet population is reported on the first attempt. Red without verification: the report counted Grade 7 beside rating 5, a state the walk never held (R1). |
| Aggregate privacy rule (RPT-03) | PASS | See RPT-03 above. `ConfigLoaderTest.testReportSuppressionThresholdIsAWholeNumberOrRefused`: unset is the approved 3, a higher value is honoured, and 0, 1, 2 or anything not a whole number refuses startup. `db-scripts.test.mjs` (migration 007 on a fresh database): overlapping dates, a release minimum below 3, a block below 3 or below its release's minimum, a zero cell, and any update are refused by the database itself. `ReportCoherenceTest.testAReleaseFreezesEveryWalkInOneCommittedState`: a save committed mid-freeze is detected and the release stores each walk's Grade beside its own rating. |
| Version isolation | PASS | `ReportServiceTest.testWalksOfAnotherVersionNeverJoinTheReport`: a walk pinned to another instrument's version is never counted and that version is not reportable (404 `REPORT_VERSION_NOT_FOUND`). |
| Lifecycle | PASS | `ReportServiceTest.testVoidedWalksNeverCountAndDraftsOnlyOnRequest`. |
| Reportable surface derived from the version | PASS | `ReportServiceTest.testTheCatalogIsDerivedFromTheVersionAndItsEngine` (every reportable single-choice item in instrument order; 24 scored ratings; Period visible exactly for the grades the engine shows it for, derived from `show_period_for_grades_6_12`); `reports.test.mjs` fails if the report code names instrument content. |

### Phase 7 correction: audit findings P7-01 and P7-02

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| P7-01 (HIGH) | RPT-03 permitted reconstruction of an individual walk's categorical responses: a report-only user could narrow a report to one walk (by answer, grade, date or a small unit) and read its answers, and the configured suppression was 0. | CORRECTED under an owner-approved rule, awaiting re-audit | The owner decided the rule (`docs/OPEN_DECISIONS.md`, "Aggregate privacy rule (RPT-03)": "K=3, frozen releases"). Report-only users read frozen, non-overlapping releases; blocks below 3 walks are never stored; every breakdown is protected by primary and complementary suppression with an exact ambiguity audit, linked breakdowns as groups, and totals built from published block cells, so subtraction yields nothing new; JSON, CSV, browser, log and audit carry the same published figures. RPT-03 row above for the tests. Red before the fix, on the real pre-fix build `0c6fa10`: a report-only user narrowed to one walk read all 12 of its categorical answers, JSON and CSV; eleven deliberate mutations of the correction each fail the privacy tests for the reason they target (`docs/evidence/phase7-correction-red-before-fix.md`). |
| P7-02 (MEDIUM) | The final live gate was not independently traceable to the delivered archive. | ADDRESSED IN THE HANDOFF, awaiting verification | The correction is one commit. It is pushed first; the clean-database gate then runs from that exact commit with the working tree clean before and after, records the commit, tree, parent, frozen-baseline ancestry and the remote branch hash, and captures its raw transcript and an environment record outside the repository (committing them would create a different, untested commit, as in CORR8-03). A source archive is made from the same commit. Each artifact is delivered with its full SHA-256 and byte size. |

### Phase 7 correction, second round: audit findings P7C-01, P7C-02 and P7C-03

The independent audit of `0c74dbd5a8a79684d38ba0b169dce2682a54fad6` ("NOT READY TO FREEZE PHASE 7").
Phase 7 is not frozen; these await re-audit.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| P7C-01 (HIGH) | The report population was said to live in fixed-name global (`##`) temporary tables shared by concurrent requests. | DID NOT REPRODUCE; HARDENED ANYWAY, awaiting re-audit | In CFML, `##` inside a string is one `#`: at run time the audited build's tables were `#icf_report_population` and `#icf_report_units`, which tempdb lists padded to 128 characters with a per-connection suffix -- connection-local -- and zero global tables exist (captured at the barrier). The new `ReportIsolationTest` barrier cases pass on the audited build unchanged, and fail there when the tables are made genuinely global (the second request waits on a schema lock). Hardened regardless, so isolation does not rest on either engine's connection handling: each computation's own GUID-named tables (one `#`, from `chr(35)`, checked on every use), built only inside a transaction, and refused on any other connection. `docs/evidence/phase7-correction-2-red-before-fix.md`. |
| P7C-02 (HIGH) | Non-overlapping dates do not make release populations disjoint: a completed walk's date stays correctable, so a released walk could be counted again by a later release. | CORRECTED, awaiting re-audit | Reproduced on the audited build: release B counted 4 walks where 3 were new (`ReportReleaseMembershipTest`, "Expected [3] but got [4]"). Now every walk a release counts is recorded in `icf.report_release_walk`, keyed by the walk, so the database refuses a second membership whatever writes it; a new release leaves out every walk an earlier one counted. Decision (fail closed, documented in `docs/DATA_CONTRACT.md`): walks stay correctable; the release that counted a walk keeps its figures; no later release counts it; a walk corrected into released dates is never released. The database also refuses a block that disagrees with its recorded walks, any change to them, and any addition after the release's own transaction. |
| P7C-03 (MEDIUM, evidence) | The final exact-commit gate transcript and environment record were not in the delivered handoff; the archive carries no git metadata. | ADDRESSED IN THE HANDOFF, awaiting verification | The gate runs again from the exact pushed commit of this round. The raw transcript, the environment record, the source archive, a git bundle of the branch (so commit, tree, parent and ancestry can be checked offline) and a `SHA256SUMS` file travel together in one handoff bundle, each named after the full commit SHA with its SHA-256 and size. None is committed (CORR8-03). |

### Phase 7 correction, third round: audit findings P7C-03 and P7C-04

The independent re-audit of `9ef9b97b5b4ee20dd51d6ca023c0841b7ca872ba` ("NOT READY TO FREEZE PHASE 7")
closed P7C-01 and P7C-02 at source level, kept P7C-03 open and raised P7C-04. Phase 7 is not
frozen; these await re-audit.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| P7C-03 (MEDIUM, evidence) | The promised handoff bundle was not received: the auditor had GitHub's download of the branch and a narrative listing hashes of files it did not contain. | ADDRESSED, awaiting verification | The bundle existed but was not what reached the auditor. The gate evidence now also travels **inside the branch**: after the gated commit is pushed and its gate passes, a separate commit adds `docs/evidence/gate/` with the raw transcript, environment record, push record, mutation transcript and `SHA256SUMS`, and changes nothing else, so any download of the branch carries it and the downloaded tree less that directory must equal the gated commit's tree. The handoff bundle, with a git bundle of the branch, is delivered as well. |
| P7C-04 (MEDIUM) | The release schema refused updates but not selective deletion: one cell, or a block with its cells and walks, could be deleted and change a release under the same identity. | CORRECTED, awaiting re-audit | Reproduced on the audited schema (`ReportReleaseTest`: one cell, a block with its cells and walks, and the whole release were all deleted; `db-scripts.test.mjs`: "one cell is never deleted" failed). Now each of the four release tables refuses DELETE before it happens (`INSTEAD OF DELETE`, 50068), the audit's enforcement option. A principal with data permissions only cannot delete a release row, or disable, drop or truncate past the guard (tested); the runtime login must be such a principal (`database/README.md`). The test-only fixture cleanup, on a development login with ALTER, switches the delete guards off inside its own transaction. |

## Phase 6 administration audit corrections (P6A-01 to P6A-04)

An independent audit of the Phase 6 administration candidate (`2a3f2ecb4f070401cba00518c0db8a2de823a29d`)
found four defects. Each was corrected on `claude/icfwalk-phase-6-admin-audit-corrections` after a
regression that failed on the candidate for the intended reason
(`docs/evidence/phase6-admin-audit-corrections-red-before-fix.md`). Verified on Lucee 6.2.8.20 with
SQL Server 2022 and Chromium only. The full gate on the code commit
`06660a238c8dd6c62cacd8a27ed970e757ef22a1` passed: Node/HTTP/Playwright 240/240, CFML 475/475, 0
failed, 0 skipped (`docs/evidence/phase6-admin-audit-corrections-release-gate.txt`). The re-audit of
those corrections led to one more correction, P6A-R01 (and P6A-R02, found while verifying it), gated
on `158debca5da2c4f3a07f602689cd08af9db9bd6e`: Node/HTTP/Playwright 240/240, CFML 483/483, `test:package` 19/19, `validate:handoff` ok; 0 failed, 0 skipped (`docs/evidence/phase6-admin-read-bound-release-gate.txt`). **Phase 6 is
frozen at that commit on the project owner's direction** (`docs/evidence/phase6-freeze.md`); Adobe
ColdFusion 2023 and SQL Server 2016 have still not been run. **The independent re-audit of that
commit passed: P6A-R01 is closed, and Phase 6 is accepted and frozen for the verified scope.**

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| P6A-01 | The router read and deserialized every request body before authentication, CSRF and the import's 5,000,000-byte limit (which the controller checked after the parse); its chunked fallback counted characters, not UTF-8 bytes; the page read a whole file before checking its size. | CONFIRMED, FIXED | `RouterBodyOrderTest` (12): the real router over a recording `FakeRequestSource` and a `SpyJsonBodyParser` -- unauthenticated, CSRF-invalid and unpermitted large or malformed requests are refused with zero body reads and zero parses; an authorized malformed or valid body over the limit is 413 `DOCUMENT_TOO_LARGE` with zero parses (declared: zero reads); 1.7 million euro signs (5.1 MB of UTF-8) are 413; within the limit is parsed once; whitespace is a body that parses to nothing; the body-member permission still authenticates first; the maintenance token is checked before the body; other routes are bounded by the 20,000,000-byte server maximum. `admin-instrument.test.mjs` (5 P6A-01 cases) on the running server, including bodies that never finish: answered 401/403/404/413 without the rest being sent. `browser-admin.test.mjs` "P6A-01 (browser)": 5,000,001-byte JSON and 20,000,004-byte workbook files refused with only an 8-byte header read, nothing sent. |
| P6A-02 | A DRAFT re-import was protected only by the page's comparison with its cached version list: the server received `{ document }` and overwrote whatever DRAFT held the label, so a stale page overwrote a newer DRAFT and a label created after the page looked was taken over; the export read metadata and document in two reads. | CONFIRMED, FIXED | `DraftReplacementConcurrencyTest` (6): a replacement naming C1 cannot overwrite C2; a create-only import queued (two-sided barrier, blocking observed in `sys.dm_exec_requests`) behind the creation of its label is refused 409 `DRAFT_REPLACEMENT_REQUIRED`; a confirmed replacement queued behind a later edit is refused 409 `DRAFT_CHANGED` with the edit byte-identical; a correct replacement succeeds once and is audited with `replacedVersionId` and `previousChecksum`; every other token is refused atomically; an export with an edit forced between its read and its use returns one state. `admin-instrument.test.mjs` "P6A-02" and the updated ADM-01 case. `browser-admin.test.mjs` "P6A-02 (browser)" (2): two sessions at C1, B saves C2, A's upload from the stale page is refused and C2 stands; a label another session created after the page loaded is asked about, and replaced only when confirmed. |
| P6A-03 | `discardDraftById` read the named row unlocked and delegated to the label-addressed discard, which deleted whatever row held the label: a request naming V1 could delete a V2 created under V1's label in between. | CONFIRMED, FIXED | `DiscardIdentityBarrierTest` (2): the delete/recreate interleaving forced through the two-sided barrier -- the request naming V1 deletes and reports V1, B is observed blocked behind it, V2 survives with no discard audited against it; the id-addressed refusals keep their codes. Existing `InstrumentScopeTest` (cross-instrument same label) and `InstrumentImmutabilityTest` still pass. |
| P6A-04 | `decodeEntities` passed any finite numeric character reference to `String.fromCodePoint` (`&#999999999;` threw `RangeError`); malformed cell references threw `TypeError`; `readWorkbook` converted only `WorkbookError`; the import handler parsed outside its guarded flow. | CONFIRMED, FIXED | `workbook.test.mjs` (4 P6A-04 cases): out-of-range, surrogate and XML-forbidden references are `XML_MALFORMED`, every legal scalar still decodes; malformed cell and row references are `CELL_REFERENCE_INVALID` naming the sheet; an unexpected shape is a structured problem. `browser-admin.test.mjs` "P6A-04 (browser)" (2): a workbook with `&#999999999;` and a file the browser cannot read each show a problem, send nothing, and the next import works. |
| P6A-R01 | Found by the re-audit of the corrections: `HttpRequestSource.readBody` asked the container's stream for a fixed 65,536 bytes per read and compared the total afterwards, so it could consume up to 65,536 bytes past the limit (5,046,272 for the import limit, 20,054,016 for the server maximum), while the records said "at most one byte past"; `RouterBodyOrderTest` used only `FakeRequestSource` and could not see it. | CONFIRMED, FIXED, CLOSED (re-audit passed) | Each read asks for `min(65536, maxBytes - total + 1)` bytes. `RequestBodyReadBoundTest` (8) runs the production `readBody` loop (`StreamedRequestSource` replaces only the stream's origin) over a real `ByteArrayInputStream`: for limits 0, 1, 2, 65,535-65,537, 131,071-131,073, 5,000,000 and 20,000,000, bodies 1 to 200,000 bytes past, and streams delivering 1 to 65,535 bytes per read, exactly `limit + 1` bytes are consumed and counted and no read asks for more than the bound; a body within the limit is read whole and byte for byte; multibyte text is bounded in bytes; through `Router.handle` an oversized chunked import is 413 after exactly 5,000,001 bytes and nothing is taken from the stream before authentication, CSRF or a declared-length refusal. Red on the unmodified loop in `docs/evidence/phase6-admin-read-bound-red-before-fix.md`. |
| P6A-R02 | Found by this pass while verifying P6A-R01, not by the re-audit: "ADM-01: the import body contract and the size cap" (`admin-instrument.test.mjs`) sent its oversized body with `fetch`. Since P6A-01 the server answers 413 from the declared length before reading and closes while the client may still be uploading, and `fetch` sometimes reported its own refused write instead of that answer: one failure in a development run (the server log shows the 413 was sent), reproduced 1 in 100 with the case's exact sequence (`fetch failed`, cause `EPIPE`). A defect of the test's client, not of the application. | CONFIRMED, FIXED | The request is sent with the file's raw-socket `rawRequest` helper -- the same bytes, headers and assertions (413 `DOCUMENT_TOO_LARGE`); 0 failures in 300 attempts of the same sequence. `docs/evidence/phase6-admin-read-bound-red-before-fix.md`, "P6A-R02". |
